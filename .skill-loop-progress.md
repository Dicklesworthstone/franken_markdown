# Skill Loop Progress
# Skill: extreme-software-optimization (ROUND 2)
# Target: franken_markdown
# Total Passes: 30
# Started: 2026-08-29T00:55:00-04:00
# Round-1 ledger: committed at 9b57fb0 (30 passes, complete)
## Status: COMPLETE — 30 of 30 passes (2026-08-29). Loop finished; final gate + push by orchestrator.
## Baseline (tests/artifacts/perf/round2-baseline-20260829T002312/)
Scenario p50/p95 (ns), 200 iters, release-perf, M4 Pro:

- html-showcase: 704,458 / 919,333
- html-large: 5,318,084 / 6,302,500
- html-code-heavy: 487,209 / 688,958
- pdf-showcase: 4,364,417 / 4,917,125
- parser-large: 6,981,500 / 9,300,334
- paragraph-1k: 102,042 / 129,125
- hyphen-corpus: 1,111,125 / 1,414,750
- font-subset: 12,000 / 12,375
- compress-corpus: 2,520,750 / 3,483,750
- pdf-large: 8,752,084 / 9,726,250

Top profiled hotspots (samply self-time): zlib_compress_with_scratch (95% compress / 33% pdf-large / 15% pdf-showcase / 8% html-large), Kerning::pair (52% pdf-showcase — per-render 128×128 AsciiWidthTables rebuild ×6 faces), malloc (29% parser-large), FontCharSet::extend_text (11% html-large), build_pdf xref (6% pdf-large), dragon f64 formatting (3% html-large).

## Missions (one lever per pass; score = Impact×Confidence/Effort)
1. ascii-tables-process-cache: hoist per-render AsciiWidthTables (pdf.rs:1418/1478) into the fonts registry keyed by face identity+weight (10.0)
2. kern-matrix-coverage-build: build 128×128 kern matrix by enumerating KernSubtable coverage instead of probing 16,384 pairs (fmd-font) (6.0)
3. pdf-zlib-scratch-share: shared ZlibCompressScratch across page-stream + font-program compression in pdf.rs build path (8.0)
4. woff1-zip-epub-scratch: thread shared scratch through woff1/zip/epub compression + touched-slot reset (6.0)
5. deflate-u32-indices: compress.rs head/prev usize→u32 (representation only, byte-identical) (6.0)
6. adler-literal-run-batching: batch literal-run Adler-32 through update_slice (8.0)
7. tounicode-cmap-append: utf16be_hex String-per-char → append_utf16be_hex into presized buffer (pdf.rs:29609) (15.0)
8. embedded-face-borrow: EmbeddedFace deep-clone of Kerning/Ligatures → Cow borrow (pdf.rs:19751) (15.0)
9. segs-adjusted-measure-once: eliminate O(k²) prefix re-shaping in build_segs_adjusted (pdf.rs:18825) (5.3)
10. shape-run-scratch: reuse Vec<char>/Vec<u16> scratch in shape_run (pdf.rs:29285) (6.0)
11. structmark-inline-paths: small-path inline for per-line/per-cell StructMark prefix Vecs (pdf.rs:26227) (4.0)
12. token-link-interning: push_text_tokens per-word String + link URI clone → intern (pdf.rs:17295) (4.0)
13. shaped-cache-single-hash: entry-API + precomputed Seg text hash for draw-side lookup (pdf.rs:19564) (3.0)
14. subset-lookup-vec: additive subset_glyphs_with_lookup returning dense Vec (fmd-font + pdf.rs) (7.5)
15. toc-fixpoint-cache: persist value-pure layout caches across TOC fixpoint passes (pdf.rs:2849) (10.0)
16. build-pdf-append-writers: format! temporaries + xref path → append writers (pdf.rs:28700+) (4.0)
17. highlight-keyword-binary-search: const-sorted keyword tables + binary_search; SQL lowercase-once (highlight.rs:319) (10.0)
18. font-charset-extend-batch: batch FontCharSet::extend_text (html.rs:1679) (6.0)
19. safe-url-range-cache: store trimmed (start,end)+safe in url cache (html.rs:262) (8.0)
20. math-cache-lru: bounded (tex,display)→MathML cache like highlight_cache (html.rs:620) (6.0)
21. dragon-float-fixed-format: find f64 Display sites in html/diagram path → fixed-point append (6.0)
22. footnote-walk-gating: lazy collect_footnote_defs gating (html.rs:1228) (8.0)
23. inline-cache-admission: two-hit admission / clone-avoidance in InlineParseCache (parse) (4.5)
24. inlines-from-chars-entry: char-slice entry avoiding String collect round-trip for nested links (parse) (3.0)
25. bracket-pair-u32: Vec<Option<usize>> → Vec<u32> sentinel (parse:4836) (8.0)
26. code-span-no-alloc: normalize_code_span clean-span fast path (parse:4120) (4.0)
27. code-body-presize: fenced/indented code String presizing (parse:1372/2195) (3.0)
28. trim-byte-classes: trim_space_tab/trim_matches char-class → byte tables (parse:4960) (8.0)
29. advance-u64-fast-path: advance_to_layout_units u64 fast path with clamp-equivalence (layout:163) (12.0)
30. math-metrics-memo: per-(face,gid) glyph metrics memo in fmd-math (faces.rs:154) (6.0)

Reserves (substitute if a mission proves invalid): MathML writer presize+stack attrs (fmd-math/mathml.rs:51), EPUB bulk copy/escape (epub.rs:150), paragraph-items presize (layout.rs:458), BitWriter u64 widen (compress.rs:69).

## Completed Passes
### Pass 1 — ascii-tables-process-cache — 2026-08-29T01:20
- Files changed: src/pdf.rs (Face.ascii → Arc + process-global ASCII_TABLE_CACHE keyed on registry-static pointer identity; owned/host faces bypass)
- Results: pdf-showcase p50 4,364,417→2,108,042 ns (−51.7% ledger / −56.4% paired), p95 −49.0%; pdf-large −5.0% paired
- Goldens: 15/15 deterministic artifacts byte-identical (incl. both PDFs); determinism + wasm-core scripts pass
- Proof: tests/artifacts/perf/round2-pass1-ascii-tables-process-cache/OPTIMIZATION_PROOF.md (check-optimization-proof.sh ok)
- Commit: c3041e2 (carried disclosed pre-existing peer hunks)
- Verdict: PRODUCTIVE
- Env note: rch silently leaves stale example binaries on retrieval failure — passes must verify binary freshness (md5/perf probe) or build with RCH_SHIM_LOCAL_IDE=1 like check-wasm-core.sh

### Pass 2 — kern-matrix-coverage-build — 2026-08-29T01:45
- Files changed: fmd-font/src/lib.rs (additive Kerning::for_each_ascii_pair: enumerate defined pairs — format-1 pair-map entries via sorted byte<->gid index, format-2 coverage glyphs with hoisted class lookups — instead of probing 16,384 cells; +2 tests), src/pdf.rs (build_ascii_tables fills the zeroed 128x128 matrix via the enumeration; comments updated)
- Results: direct lever measurement 319,438→53,045 ns per face matrix build (6.02x, probe over 8 bundled kerned faces, matrices verified identical in-probe); Plex Regular emits 818 values vs 16,384 probes. End-to-end pdf-showcase p50 +1.6%/p95 +4.2%, pdf-large p50 −3.0%/p95 −12.7% (paired-interleaved, ≤10% noise envelope; bundled scenarios amortize the build to ~0 after pass 1 — the win lands on host-font per-render rebuilds ~1.6 ms/render and first-touch, as the mission predicted)
- Equality: fmd-font test asserts enumeration == brute-force pair() over all 16,384 ASCII pairs on bundled IBM Plex/CM/Noto faces + synthetic first-match/duplicate-gid/zero-shadowing test; golden_output passes; 10/15 deterministic artifacts byte-identical (both PDFs; 5 differing = documented wall-clock telemetry)
- Proof: tests/artifacts/perf/round2-pass2-kern-matrix-coverage-build/OPTIMIZATION_PROOF.md (check-optimization-proof.sh ok); determinism + wasm-core scripts ok
- Commit: 7fd04c4 (staged hunks purely this pass's; peers' other files left unstaged)
- Verdict: LANDED-DE-RISK (end-to-end noise; direct 6.02x on the replaced path, de-risks host-font renders and first-touch, strictly less work for equal output)

### Pass 3 — pdf-zlib-scratch-share — 2026-08-29T02:20
- Files changed: src/compress.rs (generation-base epoch scheme: head/prev store base+position, base starts 1 and advances per call; cand >= base invalidates stale/virgin entries = fresh-NONE equivalence; grow-only zero-fill init replaces 256 KiB sweep), src/pdf.rs (font-program loop reuses one scratch per render; page-stream per-worker scratches confirmed already shared)
- Results (paired interleaved 5×200): pdf-large p50 −7.7%/p95 −11.0%; compress-corpus p50 −22.4%/p95 −25.2% (from table-init change alone — scenario uses one-shot path); pdf-showcase p50 −5.7%/p95 −33.2%
- Measured rejections: touched-slot list (+25-30% regression), local-position prev w/ branch-on-load (+29%) — data in proof file
- Equality: new unit test scratch_reuse_matches_fresh_zlib_bytes 41/41; compress-corpus.lengths golden (fnv1a64 of every compressed payload) byte-identical; both PDFs identical; determinism + wasm-core ok
- Proof: tests/artifacts/perf/round2-pass3-pdf-zlib-scratch-share/OPTIMIZATION_PROOF.md (check-optimization-proof.sh ok)
- Commit: content at HEAD via peer batch-sweeps c18fda4 (pdf.rs) + b0380a0 (compress.rs; also carries peer checked_add hardening) — disclosed
- Verdict: PRODUCTIVE
- Ledger note for later passes: peer batch-commits sweep the tree; attempt own commit, else verify content-at-HEAD and record hashes

### Pass 4 — woff1-zip-epub-scratch — 2026-08-29T03:10
- Files changed: src/compress.rs (ZlibCompressScratch pub additive), src/woff1.rs (encode_woff1_with_scratch reuses one scratch per font), src/zip.rs (ZipWriter private scratch, deflated_entry extracted), src/html.rs (embedded_font_css lends one woff_scratch to all faces)
- Results (paired 5×200): html-showcase p50 −30.2% (770,500→537,625 ns), p95 −20.0%; html-large +2.1% = noise (woff1 is 5 faces vs 1 MiB body)
- Equality: NEW woff1 + zip byte-equality tests (shared vs fresh scratch) on top of pass-3 invariant; epub_test 13/13; goldens incl. embedded WOFF1 bytes identical
- Proof: tests/artifacts/perf/round2-pass4-woff-zip-epub-scratch/OPTIMIZATION_PROOF.md (ok)
- Commit: peer sweeps f61ec23 + a94f9ef, content verified at HEAD
- Verdict: PRODUCTIVE

### Pass 5 — deflate-u32-indices — 2026-08-29T03:45
- Files changed: src/compress.rs (head/prev → Vec<u32>; base → u64; generic deflate_fixed_lz77<Idx> with usize fallback for ≥4GiB inputs; u32-domain reset guard; +2 unit tests)
- Results (paired 5×200): compress-corpus FLAT (+0.8% p50, metrics disagree in sign — expectation not confirmed, reported with data); pdf-large p50 −4.7%/p95 −4.5% (5/5 rounds, non-overlapping ranges — chain-walk cache footprint when interleaved with render work); steady-state scratch 256KiB+8n → 128KiB+4n
- Equality: goldens byte-identical incl. compress-corpus.lengths; compress 43/43, woff1 5/5, zip 1/1; determinism + wasm32 build ok
- Proof: tests/artifacts/perf/round2-pass5-deflate-u32-indices/OPTIMIZATION_PROOF.md (ok)
- Commit: 10e0d2f (own commit, clean)
- Verdict: PRODUCTIVE (pdf-large win + footprint halved; compress-corpus flat honestly reported)

### Pass 6 — adler-literal-run-batching — 2026-08-29T04:20
- Files changed: src/compress.rs (literal-run tracking flushed via update_slice at match interruption + stream end; pre-loop 0-2 bytes as one run; update_byte deleted — one feeding primitive remains; +unit tests incl. NMAX-straddling segmentations)
- Results (paired 5×200): compress-corpus p50 −0.8% median, after-faster 8/8 rounds (~1% honest win, kept); pdf-large flat
- Equality: adler trajectory equivalence proven segment-by-segment; goldens identical; 44/44 compress tests; determinism + wasm ok
- Proof: tests/artifacts/perf/round2-pass6-adler-literal-runs/OPTIMIZATION_PROOF.md (ok)
- Commit: eec68e7 (own commit, clean)
- Verdict: PRODUCTIVE (small but consistent)

### Pass 7 — tounicode-cmap-append — 2026-08-29T04:50
- Files changed: src/pdf.rs (tounicode_cmap: BfcharUni Copy enum, one presized String via append helpers, CMAP_PREAMBLE/FOOTER consts, utf16be_hex deleted)
- Results: tounicode stage p50 −62.5% (2000→750 ns), per-round total −53.5%; end-to-end sub-noise (−0.5/−0.9%) as arithmetic predicts — hygiene win, honestly reported
- Equality: both PDF goldens + all 10 deterministic artifacts byte-identical; determinism + wasm ok
- Proof: tests/artifacts/perf/round2-pass7-tounicode-cmap-append/OPTIMIZATION_PROOF.md (ok)
- Commit: e3c1cd3 (own commit, clean)
- Verdict: PRODUCTIVE

### Pass 8 — embedded-face-borrow — 2026-08-29T11:25
- Files changed: src/pdf.rs only (EmbeddedFace<'a> borrows kern/lig as &'a Kerning/&'a Ligatures from the render's Faces registry instead of deep-cloning per used slot per render; 14 signatures +<'_>, SvgTextDrawResources ties inner lifetime to 'a, Lookup::get split <'s,'f>; 47 lines)
- Results: direct lever — 129 allocs + ~278 KiB clone traffic per 3-face render deleted, ~8.7 us cloning cost → 0 (clone-cost-microbench.txt, counting allocator, all 10 bundled faces measured); end-to-end paired 5×200 under extreme ambient load: pdf-showcase p50 −6.5% (4/5), pdf-large p50 −10.1% (4/5) — favorable but mostly load-pairing luck (lever is 0.06-0.34% of p50 by arithmetic, reported honestly)
- Equality: before/after binaries byte-identical on ALL 10 deterministic artifacts (binding proof); only the 5 by-construction wall-clock telemetry jsonl differ; determinism + wasm32/no-default ok (run in reconstructed after-binary source state — live tree was mid-edit by a peer's layout.rs WIP)
- Baseline drift vs round2-baseline goldens attributed to peer output-affecting svg.rs (double-header-paint elimination) + compress.rs co-committed in 0fa9b7a, present in BOTH paired binaries — not this lever
- Proof: tests/artifacts/perf/round2-pass8-embedded-face-borrow/OPTIMIZATION_PROOF.md (check-optimization-proof.sh ok)
- Commit: swept into peer batch commit 0fa9b7a mid-verification (with svg/compress/layout peer work); content verified at HEAD, no re-commit per protocol
- Verdict: PRODUCTIVE

### Pass 9 — segs-adjusted-measure-once — 2026-08-29T12:35
- Files changed: src/pdf.rs (build_segs_adjusted rewritten around run batches: extent pre-scan → exact-capacity String → SegRunShaper incremental single-pass prefix-width measurement (Ascii prefix-sum route / General settled-ligature-output + pending route) → running-max width fold with per-token f32 advances → one ensure_width_cached per run; Face::glyph_advance_term/glyph_kern_term mirror shaped_glyph_width's terms; +5 tests incl. differential vs verbatim old-emitter replica and all-token-splits shaper sweep; +ignored in-process A/B perf micro), fmd-font/src/lib.rs (additive Ligatures::max_rule_len settle-window accessor + test)
- Results (direct lever, in-process paired A/B, ambient-immune): justified body text 64×12 runs −28.0% (412,459→296,916 ns); extra-carrying long runs (applies_to_words/CJK worst case) −87.9% (658,875→79,875 ns, 8.2×) — the O(k²)→O(k) collapse; transient prefix WidthCache keys eliminated (cache-hygiene test). End-to-end 5×200 under load≈20: pdf-large p50 −0.2%/p95 −5.8%; pdf-showcase +3.7% p50 — inside the demonstrated ±5-8% ambient noise floor (paragraph-1k control on UNTOUCHED code shows +0.1/+0.2/+7.3% pair deltas); honestly reported as sub-noise
- Equality: before/after binaries (shared compilable snapshot state S = f4a3913 + peer expansion-permille dirty files; before md5 reproduced exactly by independent rebuild) byte-identical on ALL 10 deterministic artifacts incl. both PDFs (which also match round2-baseline digests — bonus); 5 telemetry jsonl differ by construction; live-tree port re-proven by differential tests against the LIVE advance-based box_width semantics (557 lib + golden_output + fmd-font 81 + clippy -D warnings all green); determinism + wasm-core ok (worktree after-state)
- Settle-rule proof: greedy ligature decisions starting ≥ max_rule_len glyphs from the end are final; the shaper re-substitutes only the trailing window per boundary, reproducing substitute(prefix)++widths term-for-term (integer LayoutUnit, same clamp); f32 op order preserved exactly (a telescoped x += width variant was caught 1-ULP off by the differential test and reverted to per-token advances)
- Proof: tests/artifacts/perf/round2-pass9-segs-adjusted-measure-once/OPTIMIZATION_PROOF.md (check-optimization-proof.sh ok); state-S snapshot preserved at /tmp/pass9-before-state/
- Commit: 9af70ba (final hash after two history rewrites from concurrent peer commits; own commit; carries peer-staged expansion-permille/gradual-demerits work that was staged into the shared index mid-flight — CHANGELOG.md, docs/MICROTYPOGRAPHY.md, src/{cli,layout,lib}.rs, tests/{gradual_demerits,microtype,pdf_inflate_helper}*.rs* — same sweep phenomenon as pass 1; my hunks: src/pdf.rs segs machinery + fmd-font max_rule_len + this ledger)
- Verdict: PRODUCTIVE

### Pass 10 — shape-run-scratch — 2026-08-29T13:05
- Files changed: src/pdf.rs (AsciiWidthTables +glyphs[128] byte→gid cmap mirror from the same builder lookups; shape_run takes the face's tables and delegates to shape_run_with_scratch: fast route for pure-ASCII text with no lig_start byte — gate proves substitute is the identity, so shaped run = table gids, no Vec<char>, no cmap search, no substitution pass; general route = original algorithm verbatim on one per-render ShapeScratch{chars,gids,subst} hoisted across all slots in the serialize cache loop; draw_svg_text/draw_seg cold fallbacks pass faces.face(slot).ascii_tables(); +differential test vs verbatim pre-change replica (14 corpora × 7 slots, fresh + reused scratch) + ignored in-process A/B perf micro), fmd-font/src/lib.rs (additive Ligatures::substitute_with_spans_into — same loop, caller-owned buffer; substitute_with_spans delegates; +dirty-buffer/reuse equality test)
- Results (direct lever, in-process paired A/B, ambient-immune): 72-segment mixed-route corpus (3 slots) −58.6% (15,709→6,500 ns); in-pipeline stage telemetry (pdf-large, binding runs): glyph_collection_and_shaping p50 −42.2% (192,417→111,083 ns), total −33.6% with identical counts/bytes. End-to-end 5×200 interleaved under EXTREME load (≈97): pdf-showcase p50 −7.3% median and after faster 5/5 rounds (exceeds the paragraph-1k control's −1.0%); pdf-large −0.6% (3/5) — inside noise, honestly reported
- Equality: before/after binaries from two frozen worktrees at a47f885 (before hand-reverted, verified 0 trace of the change and pdf.rs diff vs pre-change blob 692cca7 = peer river-penalty hunks + import reflow only; md5s c45eacaf…/6519cf48…) byte-identical on ALL 10 deterministic artifacts; 5 telemetry jsonl differ by wall-clock fields only (non-timing fields identical); golden_output passed in frozen after-worktree AND live tree; differential test green; determinism + wasm-core ok (frozen after-worktree)
- Gate soundness: substitute can only fire at position i when gids[i] ∈ rule_start_glyphs; pure-ASCII ⇒ every gids[i] == tables.glyphs[byte] and the gate excludes any byte with lig_start, so no rule can fire and spans are all 1 (no lig_uni) — exactly the fast route's output; TAIL_NOGLYPH debug toggle keeps the general route (table mirrors the unchecked cmap)
- Proof: tests/artifacts/perf/round2-pass10-shape-run-scratch/OPTIMIZATION_PROOF.md (check-optimization-proof.sh ok); reconstructed pre-change state preserved at /tmp/pass10-before/
- Commit: swept mid-flight into peer batch commits (fmd-font: b62c538 + 692cca7; pdf.rs: 44a0ad1, 59caedf, b36745a; +peer formatting c5ff4f6/af415fe); content verified at HEAD a47f885 via the frozen-worktree pair; no re-commit per protocol
- Verdict: PRODUCTIVE

### Pass 11 — structmark-inline-paths — 2026-08-29T13:55
- Files changed: src/pdf.rs only (StructMark.path Vec<SElem> → hand-rolled SmallPath{Inline{elems:[SElem;8],len:u8},Spilled(Vec)} with Deref<Target=[SElem]> so build_struct_tree is untouched; INLINE_PATH_CAP=8 picked from measured data — cfg(test) histogram in build_struct_tree over showcase/pdf-large-gen/table-list-heavy/README through the real pipeline: depths 1-8 cover 100%/99.3%/99.1%/96.1% of marks, depth 10 the only observed spill; container_prefix (née container_prefix_with_extra, extra_len dropped as meaningless) sorts quote/list containers in a fixed [(usize,Container);8] stack array with Vec fallback >8 — kills the second per-quote/list-line malloc; per-cell clone_prefix_with_extra deleted → SmallPath::from_slice + 3 pushes; retained_pdf_bytes pins its per-mark term to STRUCT_MARK_RETAINED_BYTES=80 (exact pre-change size_of::<StructMark()>, rustc-verified) so pdf_heap_ceiling decisions stay byte-identical; SKey +Debug derive; +differential tests vs verbatim pre-pass-11 reference builders (11 container cases incl. both spill routes + 4 table contexts incl. depth-9 spill, asserting Inline/Spilled variant selection) + spill-roundtrip unit + ignored in-process A/B perf micro)
- Results (direct lever, in-process paired A/B, ambient-immune, 2000 iters × 12 path builds, sink-equality asserted): reference 998,958 vs inline 685,791 ns (−31.3%); repeat −34.2% (≈41.6→28.6 ns/build — the deleted per-line/per-cell malloc+free, plus the containers-Vec malloc on quote/list lines). Stage telemetry (binding pdf-large runs, directional): page_content_stream_generation p50 −4.1%. End-to-end 2×5×200 interleaved, moderate load (9-21): pdf-large p50 −1.4% (4/5 rounds after-faster, invocation 2), pdf-showcase ±0.5% — but paragraph-1k control (zero changed code) swings −19%…+47%, so end-to-end honestly reported as sub-noise; the micro is the decisive instrument
- Equality: before/after binaries from two frozen worktrees at d26e7e1 (after = +pass-11 pdf.rs only; live pdf.rs diff verified to contain exactly my hunks, peer mcp/wasm files excluded) byte-identical on ALL 10 deterministic artifacts (pdf-showcase fb05e3c3…, pdf-large 97898c9b…); 5 telemetry jsonl differ by wall-clock fields only, plus page_content_buffer_preallocation output_bytes 2,780,150→3,425,510 which truthfully reports the marks Vec's new ×320 physical reservation (telemetry, not output); golden_output + pdf_chunk_test (ceiling path) + clippy --lib --tests clean in live tree; determinism + wasm-core ok (frozen after-worktree /tmp/pass11-after)
- Proof: tests/artifacts/perf/round2-pass11-structmark-inline-paths/OPTIMIZATION_PROOF.md (check-optimization-proof.sh ok); depth-measurement.txt records the N-choice data
- Commit: ee6aa4b (own commit, clean, single file)
- Verdict: PRODUCTIVE (direct −31%/−34%; allocation churn per marked line + per table cell + per quote/list line deleted; ≥96% of marks now zero-heap)

### Pass 12 — token-link-interning — 2026-08-29T14:25
- Files changed: src/pdf.rs only (Tok.link/CellRun.link/TokMeasureRun.link: Option<LinkTarget> → Option<u32> interned index; new LinkIntern{targets:Vec<LinkTarget>, ids:HashMap<LinkTarget,u32>} in LayoutCx — content-keyed dedup makes index equality ≡ link-target equality, so all slot/link/strike merge + justification-extent compares became u32 compares; tokenize interns safe_pdf_link(dest) once per Inline::Link span instead of cloning the URI String per token; resolve_seg_link is the single Seg-construction resolution point, so draw paths/annotations see identical owned targets; threading through cell_tokens/TableLayoutSpec/table_cell_measure/wrap_perf_tok_hash (digest-identical keys via resolved bytes)/build_segs*/layout_inlines_greedy; LinkTarget +Hash/Debug, Fill +Debug; Tok 64→40 bytes; +differential test vs verbatim pre-change tokenizer, +same-URI-merge invariant test, +ignored in-process A/B perf micro; Cow<'str> text extension skipped — toks are cloned/spliced/stored past the source borrow, borrow can't be proven, noted in proof)
- Results (direct lever, in-process paired A/B, ambient-immune, 9 interleaved rounds ×3 invocations): link-heavy tokenize+merge (24 links × 20 words, 100-char URIs, dedup repeats) 51,000 → 34,625 ns (−32.1%; repeats −30.3%/−25.7%, median ≈−30%) — the deleted per-token URI malloc+memcpy and string-compare merges. End-to-end 5×200 interleaved under EXTREME load (24-42): pdf-large p50 median −3.7% (after faster 4/5), pdf-showcase +2.4% (1/5) — paragraph-1k control bounds pair noise ≈±1% with a +15% round-5 ambient ramp on both sides, showcase honestly reported sub-noise; micro is the decisive instrument
- Equality: before/after binaries from two frozen worktrees at bd133c5 (before md5 b8d2a941…, after d666080b…, RCH_SHIM_LOCAL_IDE=1 + isolated per-worktree CARGO_TARGET_DIR) byte-identical on ALL 10 deterministic artifacts incl. both PDFs (binding: link annotations carry URI text); 5 telemetry jsonl differ in *_ns fields only (key-by-key verified); interned_links merge test locks the critical dedup-equality property; lib 708 + golden_output + pdf_chunk_test green; clippy 0 warnings; determinism + wasm-core ok (frozen after-worktree)
- Proof: tests/artifacts/perf/round2-pass12-token-link-interning/OPTIMIZATION_PROOF.md (check-optimization-proof.sh ok)
- Commit: 626c236 (own commit, clean, single file)
- Verdict: PRODUCTIVE (direct −30% on link-heavy tokenize; per-token URI allocation deleted; every token clone in the pipeline cheaper)

### Pass 13 — toc-fixpoint-cache — 2026-08-29T14:40
- Files changed: src/pdf.rs only (TOC branch of layout(): ONE LayoutCx now spans the whole page-number fixpoint instead of a fresh construction per pass; value-pure caches carried across passes — width_cache (key slot+size→text→exact shaped-width LayoutUnit), hyphen_cache (lang:word → intrinsic break points), simple_paragraph_cache (key text+indent+measure+size+gap+kind+policy, templates scrubbed to FlowMark::default() at insert, fully re-stamped by mark_flow at hit), table_layout_cache (group scrubbed at insert, re-stamped by stamp_table_layout_group at hit), paragraph_scratch + line_breaks/line_toks/glue_adjustments/code_highlight_spans workspaces (cleared at every consumer entry), links (content-keyed intern ids, internal-only, equal ids ⇔ equal targets), toc_entries (moved in once, read-only); per-iteration RESET to exact fresh-construction values: toc_page_map (the fixpoint variable), next_flow/next_bg (numbering must restart per pass), pending_page_break, list_stack; full field audit in the code comment at layout() + the proof doc. !has_toc branch byte-identical/untouched)
- Surface discovery (mission ask): NEITHER harness PDF scenario exercises the fixpoint — showcase.md has no TOC marker (grep: zero hits), generated_pdf_large() emits no marker, opts leave toc:false, and the golden corpus (code/lists/prose/quote-and-rule/table) has no TOC doc → golden_output does NOT cover the fixpoint; binding TOC proof is a dedicated corpus run: harness --scenario corpus --corpus toc-corpus.md (64,091 B, [[TOC]], 120 sections, repeated identical paragraphs+tables+code, archived as toc-corpus.md; 365,140-B corpus.pdf, 36 pages). Pass-count argument: pass 1 starts page_map all-1, headings land on pages 2..36 → new_map≠init → ≥2 passes guaranteed (typically 2-3)
- Results (direct lever = that TOC corpus, 5×200 paired interleaved rounds, before/after alternating, load 17-21): render-pdf p50 median 13,258,917 → 9,749,083 ns (−26.5%, after faster 5/5; R1 before 27.1ms = warmup outlier), p95 median 14,492,083 → 11,529,542 ns (−20.4%). No-TOC surfaces neutral as expected (code path untouched): pdf-large +0.0%, pdf-showcase −0.1%, paragraph-1k control −0.3%
- Equality: before/after binaries from two frozen worktrees at fbf56ad (before md5 84df9bd4…, after ba0bbadc…, RCH_SHIM_LOCAL_IDE=1 + isolated per-worktree CARGO_TARGET_DIR, freshness mtime+md5) byte-identical on ALL 10 deterministic artifacts (pdf-showcase fb05e3c3…, pdf-large 97898c9b…) AND on the TOC corpus (corpus.pdf baef5c52… both sides, corpus.html a2a6f15f… both sides); 5+2 telemetry jsonl differ in *_ns fields only (key-by-key verified); golden_output 1/1 (live tree), pdf_toc_test 6/6 incl. pdf_toc_two_pass_convergence_multi_page, cargo test --lib 704 passed / 0 failed (frozen after-worktree); clippy --lib clean; determinism + wasm-core ok (frozen after-worktree /tmp/pass13-after)
- Proof: tests/artifacts/perf/round2-pass13-toc-fixpoint-cache/OPTIMIZATION_PROOF.md (check-optimization-proof.sh ok); bench-interleaved-5rounds.txt + bench-summary.txt + toc-corpus-checksums.txt + perf-samples-{before,after}.jsonl
- Commit: 18c07b6 (own commit, clean, single file)
- Verdict: PRODUCTIVE (−26.5% p50 / −20.4% p95 end-to-end on the genuine fixpoint surface — every fixpoint pass after the first previously re-measured every word, re-hyphenated every word, and re-laid-out every repeated paragraph/table from empty caches)

### Pass 14 — subset-lookup-vec — 2026-08-29T14:55
- Files changed: fmd-font/src/lib.rs (+127/−6) + src/pdf.rs (+17/−9). subset_core built BOTH a dense new_of_lookup Vec and a parallel BTreeMap<u16,u16> remap; subset_glyphs returned the map, forcing pdf.rs to iterate it and rebuild an equivalent dense map_lookup vec. Now subset_core builds only the dense table; ADDITIVE pub API Font::subset_glyphs_with_lookup(&[u16],&[char]) -> Option<(Vec<u8>,Vec<u16>)> returns it (pub const MISSING_GLYPH_REMAP = u16::MAX sentinel; pub'd with docs); subset_glyphs keeps its exact signature by reconstructing the identical map (ascending old-gid projection of the dense table); Font::subset unchanged in shape. pdf.rs: both call sites → subset_glyphs_with_lookup; lig_uni re-key through the dense table (sentinel-gated ≡ map.get Option semantics); map_lookup = the dense vec REUSED IN PLACE (sentinel→0 normalize + truncate to num_glyphs, byte-equal to the old zeroed-vec scatter) — BTreeMap build + iteration/rebuild + one allocation deleted. fmd-font is published: change strictly additive (franken_manim safe)
- BTreeMap iteration-order audit (mission ask): pdf.rs consumed the map in exactly 2 places — lig_uni re-key via map.get (point lookup, order-free) and the map_lookup scatter (ascending old gid ≡ dense ascending-index order); both locked by new unit test subset_glyphs_with_lookup_matches_btreemap_remap on all 9 bundled faces (5 CM + 4 Plex): same bytes, same pairs, same ascending order, same coverage count across empty seed / .notdef-only / composite-closure (asserted sub.num_glyphs>2) / out-of-order+duplicate+out-of-range gids
- Results (direct lever = font-subset scenario, binary pair, 5×200 interleaved, load 16-35): p50 median 12,500→10,792 ns (−13.66%, after-faster 4/5), p95 16,542→15,500 (−6.3%). In-process micro (21 interleaved windows × 300 iters, ambient-immune): whole subset call −9.33%..−15.69% p50 (plex/cm realistic+large; conservative — new subset_glyphs pays map rebuild + sentinel scan ≥ old in-core map build); pdf-side map_lookup construction −27.7%..−68.5% (585→423, 863→315, 406→222, 697→219, 435→244, 715→248 ns; new-side model pays a clone the real in-place path does not). PDF end-to-end honestly sub-noise: pairing-1 (load→35) showcase +14.4% w/ p95 swinging 2.85→12.3ms = ambient; pairing-2 re-run (load 22-29) showcase +2.45%, pdf-large +3.01%, paragraph-1k control itself −5.25%/+3.13% → inside control band, matching the ~0.5-1.5% mechanical expectation
- Equality: before/after binaries from frozen worktrees at a1ad231 (before md5 126609ff…, after 4b801865…, RCH_SHIM_LOCAL_IDE=1 + isolated per-worktree CARGO_TARGET_DIR, freshness mtime+md5) byte-identical on ALL 10 deterministic artifacts incl. font-subset.ttf 39b2e742…, pdf-showcase.pdf 3ce0bb12…, pdf-large.pdf 4103b9dc… both sides; 5 telemetry jsonl differ in *_ns fields only (key-by-key audited, zero non-ns diffs); fmd-font suite 100/100 (83 lib + 12 + 5) incl. new equality test; golden_output 1/1, font_test 25/25, kern 2/2, lig 3/3, fonts 4/4 at HEAD post-sweep; clippy clean (fmd-font + lib); determinism + wasm-core ok (frozen after-worktree)
- Proof: tests/artifacts/perf/round2-pass14-subset-lookup-vec/OPTIMIZATION_PROOF.md (check-optimization-proof.sh ok); bench-interleaved-5rounds.txt + -rerun + bench-summary.txt + micro-direct-lever.txt + pass14_micro.rs.txt + golden_checksums_{before,after}.txt
- Commit: swept mid-flight into peer batch commit 996aec20cd6f589d6545ed57d5bdd06efaa160b2 (fmd-font +127/−6, pdf.rs +17/−9; content verified byte-identical to the frozen after-worktree files at HEAD; later peer commits 5dc45cd touch parse/html only); no re-commit per protocol; rollback = `git show 996aec2 -- fmd-font/src/lib.rs src/pdf.rs | git apply -R`
- Verdict: PRODUCTIVE (direct −13.7% font-subset p50, −9..−16% whole subset call, −28..−68% pdf-side lookup rebuild; per-face-per-render the subsetter stops allocating a BTreeMap node per kept glyph and pdf.rs stops re-deriving the table it already had)

### Pass 15 — shaped-cache-single-hash — 2026-08-29T15:30
- Files changed: src/pdf.rs only (+455/−28, co-authored with an EXTERNAL peer agent — see below): (a) collect loop contains_key+insert (2 traversals, 2 SipHash passes over the text per ref, miss = String alloc + 3rd hash) → ONE entry() traversal; (b) Seg.text_hash: u64 (FNV-1a 64 via seg_text_hash, computed at every construction site, 15 non-test + tests; Seg 72→80 bytes measured+documented); ShapedRunCache HashMap<String,ShapedRun> → HashMap<u64,ShapedRun,U64PassThroughBuild> (pass-through hasher: the fnv digest IS the hash, no SipHash-on-u64); ShapedRun.text keeps the text (moved from the key, same 1 alloc/run) verified byte-for-byte on every hit by shaped_run_lookup + the collect-loop matches! guard → digest collision (p≈2^-64) falls back to shape_run on the ACTUAL text, never wrong glyphs (locked by simulated-collision unit test); draw_seg keys on seg.text_hash (zero draw-side text hashing), draw_svg_text computes per call; new shaped_cache_tests (collision guard, fnv1a64 correctness, ignored in-process A/B micro)
- Co-authorship: this pass landed the core (Seg field, helpers, hasher, entry-API loop, draw lookups, tests); an external peer agent (not Main's) completed the mechanical construction-site lines using the landed helper, populated ShapedRun.text at both shape_run return sites, switched to with_capacity_and_hasher, reformatted the tests, and batch-committed everything inside 73afa85 ("... optimize seg text hashing") alongside their unrelated pareto/conformance work. Adopted per deconflict protocol; deconflict was coordinated with Main (their only live agent was me)
- Results: direct lever (in-process interleaved A/B, 720 collect refs + 720 hit-dominated lookups, 16 distinct texts, 6 runs): 13.1–15.3 µs → 7.6–8.8 µs, −36.4%..−46.3% (median ≈−42%) — the deleted per-ref/per-draw SipHash passes. End-to-end 5×200 paired (load ≈10): pdf-large p50 −0.46%/p95 −0.76%, pdf-showcase p50 −0.76%/p95 −3.43%, paragraph-1k control −0.14%/+0.53% (tight); under EXTREME load 58: pdf-large −5.42%/−14.50% (after-faster 3/5), showcase −2.47%/−2.02%, control swinging ±8–21% — favorable, honestly inside the ambient envelope there
- Equality: before/after binaries from frozen worktrees at 73afa85 (after = commit + cfg(test)-only micro-test fix; before = commit with the 34 mission hunks reverse-applied, 0 text_hash lines remain; md5s before 7d892116… after 4d99bab4…, RCH_SHIM_LOCAL_IDE=1 + isolated per-worktree CARGO_TARGET_DIR, freshness mtime+md5; an earlier pre-commit A/B pair rebuilt at commit level produced BIT-IDENTICAL binaries, proving commit vs frozen state differ only in cfg(test)/doc) byte-identical on ALL 10 deterministic artifacts incl. both PDFs + font-subset.ttf; 5 telemetry jsonl differ in *_ns only (key-by-key audited; shaped-cache hit/miss stage COUNTS equal — counter-equivalence proven empirically); golden_output 1/1; cargo test --lib 570/570 in live tree AND committed after-worktree; clippy --lib exit 0 zero warnings; determinism + wasm-core ok (frozen after-worktree)
- Proof: tests/artifacts/perf/round2-pass15-shaped-cache-single-hash/OPTIMIZATION_PROOF.md (check-optimization-proof.sh ok); bench-interleaved-5rounds.txt + -load58.txt + micro-direct-lever.txt + golden_checksums_{before,after}.txt + mission-hunks.patch (rollback artifact) + micro-test-fix.patch
- Commit: mission content at HEAD via peer sweep 73afa85 (verified: 41 text_hash lines, entry-API, ShapedRun.text, tests present at HEAD); this pass's own commit lands the micro-test expected_hits fix + this ledger entry only (peer's unrelated 420→140 table-test hunk left unstaged)
- Verdict: PRODUCTIVE (direct −42% on the cache-op pair; per render: one full-text SipHash deleted per segment ref and per drawn/svg segment, replaced by one fnv1a at Seg construction + pass-through u64 probes; collision-verified, byte-identical output)

### Pass 16 — build-pdf-append-writers — 2026-08-29T15:50
- Files changed: src/pdf.rs only (+81/−79, own clean commit 2854681): build_pdf's last per-object format! temporaries → direct appends. (1) image XObject loop: per-image format! ×4 (main dict incl. inline `N 0 obj` header, /DecodeParms predictor string, /SMask ref string, SMask XObject dict) → append_pdf_object_header + extend_from_slice fragments + append_decimal_usize/append_decimal_u64/append_pdf_object_ref_bytes straight into buf (0 allocs/image vs 2–4); (2) annotation loop: annotation_dict(..)->Result<String> (up to 4 nested format!s/annot: rect, /StructParent, outer dict with /A or /Dest) → append_pdf_link_annotation_object(out,offsets,object_id,..)->Result<()> following the append_pdf_*_object convention, preserving exact template order /Rect[..] /Border [0 0 0] /F 4? target-part /StructParent? >> (check_uri_action still gates /A; Err path discards buf as before); one #[allow(clippy::too_many_arguments)] per repo convention. One-shot doc-level format!s (catalog/pages/outline-root/struct-root/info/PDF-A) left alone per mission
- Cited counts (mission ask): pdf-large = 0 images + 0 links → converted sites cost 0 format!s/render (only ~7 one-shot remain); pdf-showcase = 0 images (no image_assets; svg dest → alt text) + 3 links → ~6 format!s/render before, 0 after; sites are cold on BOTH stock scenarios (paired results honestly null) and hot exactly on image/link-bearing docs — measured by the micro
- Results: direct lever (TEMP micro test, identical both trees, deleted pre-commit: 1,200 distinct images = 600 RGB fast-path /DecodeParms + 600 RGBA /SMask+SMask-objects, 400 URI + 40 fragment annots, tagged, 1,575,883 B out; 400 wall + 200 profiled iters/run): pdf_object_serialization_total p50 3.999→3.704 ms = −7.4% (7/7 pairs after-faster, −5.4..−15.3%; p10 −7.3%), whole-render wall p50 10.472→9.788 ms = −6.5% (6/7). Paired 5×200: pdf-large median p50 8.19→7.71 µs and showcase 2.282→2.290 µs — both inside ambient noise as predicted (sites cold)
- Equality: before = frozen tree at 581d240 (git show HEAD:src/pdf.rs into byte-identical copy; only-src-diff verified), after = +diff; binaries RCH_SHIM_LOCAL_IDE=1 + isolated CARGO_TARGET_DIR (md5 200fdb8b… / 730253b6…, +final rebuild ed52c7bf… after lint-only attr, all 10 artifacts re-verified identical). ALL 10 deterministic artifacts byte-identical (pdf-large 4103b9dc…, pdf-showcase 3ce0bb12…) AND the micro doc byte-identical (fnv31 0x8e468febc902a086 both sides — pins predictor/smask/URI/fragment/StructParent paths); golden_output 1/1; scoped suites 646/646 (pdf 211, branch_low 136, low2 164, high 66, embed 11, svg 11, pdf_a 10, toc 6, a11y 12, lib_api 19); clippy --lib release-perf exit 0 ZERO warnings; determinism ok; wasm-core ok
- Proof: tests/artifacts/perf/round2-pass16-build-pdf-append-writers/OPTIMIZATION_PROOF.md (check-optimization-proof.sh ok); bench-pdf-large-5rounds.txt + bench-pdf-showcase-5rounds.txt + micro-direct-lever.txt + golden_checksums_{before,after}.txt + clippy-final.txt
- Commit: 2854681e91954f004363ff7b4cc164e307ced2fc (own commit, clean, single file; peer ios/ wasm changes untouched/unstaged)
- Verdict: PRODUCTIVE on image/link-bearing documents (−7.4% build_pdf serialization, −6.5% whole render, 7/7 pairs; deletes 2–4 heap Strings per image + up to 4 per annotation); NULL on stock corpora by construction — the region's remaining stock-corpus cost was already stream-converted by round-1 c120dbb, so this closes the mission's per-object tail

### Pass 17 — highlight-keyword-binary-search — 2026-08-29T16:30
- Files changed: src/highlight.rs only (+598/−368, own clean commit af749fd): keyword/type table membership for identifier classification — every identifier in a Generic-lexer block (plus CSS_KW / MERMAID_KW / MERMAID_TY lexers) linearly scanned the whole table (`contains`, or per-entry `eq_ignore_ascii_case` for SQL/PowerShell/Mermaid); a MISS (the common case) walked all 8..43 entries. Now: `KwTable{entries,offsets,fold}` — tables grouped AT COMPILE TIME by (case-folded) first byte (`const fn group_by_first_byte` counting sort stable in source order + `const fn kw_offsets_slice` [u16;129]); membership = one offset-pair probe + scan of the entry's own bucket (largest bucket anywhere = 6). Dead `Rules.case_insensitive` folded into `KwTable.fold` (field + 12 arm initializers deleted); inline JSON/TOML/INI/YAML literals → JSON_KW/BOOL_KW/YAML_KW consts; SQL_KW's lowercase tail (13 entries) was exactly the case-folded duplicate of its uppercase block and was dropped (52→39; membership under the folded compare unchanged — pinned by tests); 4 permanent `keyword_table_tests` (bucket-partition invariants via lexer() so inline tables are covered, differential membership vs a verbatim linear replica over 7 probe shapes × every entry × every table, empty-table semantics, mixed-case SQL/PS/Mermaid end-to-end incl. the formerly-duplicated `select`/`from`/`join`/`on` forms)
- MISSION DEVIATION (documented, measurement-driven): the prescribed const-sort + `binary_search` was implemented FIRST and measured +8%..+286% SLOWER than the status-quo linear scan on EVERY table (in-process interleaved A/B, 24 windows × 60 reps × ~400-word corpus/table; corroborated end-to-end: corpus-html +6.47%, after-faster 0/5 rounds) — log-n unpredictable branches lose to ~2-4-cycle length-check rejections over 8-43 L1-resident strings on this M4. Shipped the first-byte bucket index instead (same lever, same call sites, same tables): −72.6%..−88.8% (median −83%), 17/17 tables, ~16.3→~2.1 ns/lookup on RUST_KW. Both micro runs recorded verbatim in micro-direct-lever.txt
- Results: direct lever (ambient-immune in-process A/B, hit-counts asserted equal every window): 17/17 tables −72.6%..−88.8%. End-to-end paired 5×200 interleaved under EXTREME load (18-20): corpus-html (196-block keyword-dense corpus, all 18 supported fence tags, archived kw-corpus.md) p50 median 1,119,459→995,750 ns = −11.05%, after-faster 5/5; corpus-pdf −4.26% (4/5); UNTOUCHED corpus-parse control +10.65% (2/5) bounds the noise band, inside which stock html-code-heavy (+1.02%/+2.52% across two 5-round sets) and html-large (+0.19%) are honestly sub-noise at this load; paragraph-1k −1.37% (5/5)
- Equality: before/after from two frozen worktrees at e2c7141 (diff −rq: differ ONLY in src/highlight.rs; RCH_SHIM_LOCAL_IDE=1 + isolated CARGO_TARGET_DIR; md5s c87f44e4…/790d8b65…; final whitespace-only lint fix rebuilt to a BYTE-IDENTICAL binary, same md5 — measurements stand for the committed source) — ALL 10 deterministic artifacts byte-identical incl. html-code-heavy.html, AND the keyword-dense corpus renders corpus.html 5f5f4785…/corpus.pdf ac70b3ed… identically on both binaries; 5 telemetry jsonl differ in *_ns fields only (non-ns fields key-by-key audited, 50/50 records); golden_output 1/1, highlight_test 60/60 (incl. approved token fixtures + 512-round stress digest, both pre-change-generated), lib 574/574; clippy --lib --profile release-perf ZERO warnings; determinism ok + wasm-core ok (frozen after tree)
- Proof: tests/artifacts/perf/round2-pass17-highlight-kw-binary-search/OPTIMIZATION_PROOF.md (check-optimization-proof.sh ok); micro-direct-lever.txt (both runs), bench-interleaved-5rounds.tsv, bench-html-code-heavy-extra5rounds.tsv, bench-summary.txt, kw-corpus.md + kw-corpus-checksums.txt, golden_checksums_{before,after}.txt
- Commit: af749fd (own commit, clean, single file; peers' ios/ wasm edits untouched/unstaged)
- Verdict: PRODUCTIVE (direct lever −83% median on 17/17 tables; −11.05% end-to-end on keyword-dense HTML render, 5/5; membership-only with byte-identical output everywhere; mission's binary-search hypothesis honestly falsified and superseded by a stronger mechanism)
- Round 2 pass 18 (font-charset-extend-batch, mission 18) — FontCharSet::extend_text bulk fold, commit 590be33, src/html.rs only (+150/-22). Lever: the round-1 shape degraded to per-char insert for the WHOLE run tail after the first non-ASCII byte (baseline: 3.8% self loop body + 5.4% tail); new shape = runs >=16B fold pure-ASCII 8-byte words (chunks_exact + SWAR 0x8080.. high-bit test) through a shared direct-mapped 4096-entry word->mask memo (pure-function cache, full-64-bit-key compare, !word key encoding so alloc_zeroed slots never false-hit), resume the bulk scan after each decoded non-ASCII char, and take the per-char reference path for short runs (no memo alloc). FontUsage owns ONE 96KB memo threaded to all five charsets. Equality: all 10 deterministic artifacts byte-identical on before/after binaries (--scenario all --iters 25; frozen worktree @765a4e0 vs main+diff, md5s 7e0bdb50…/8a041045…), 6 telemetry jsonl differ in *_ns fields only (977 fields audited); golden_output 1/1; font_char_set 3/3 incl. new accumulation + >4096-distinct-word memo-collision differential; determinism ok; wasm-core ok; cargo check --lib zero warnings. Perf: html-large paired 5x200 interleaved p50-median 5,008,208->4,898,500 ns = -2.19% after-faster 5/5 under ambient load 24-33 where the UNTOUCHED parser-large control drifts -1.83% (noise floor ~±2%); decisive ambient-immune direct-lever micro (standalone rustc A/B, equality asserted): -62.1% showcase-lines shape, -63.6% mixed, -54.5% ascii-repeated, -28.6% ascii-new, -21.8% cjk-dense. REJECTED intermediates (measured): plain segment-resume alone was +18..+26% on pure-ASCII (manual index bounds checks), 128-entry u128 table alone -2..-21%, memo-8192 within noise of 4096 at 2x memory. Proof: tests/artifacts/perf/round2-pass18-charset-extend-batch/OPTIMIZATION_PROOF.md (check-optimization-proof.sh ok). Verdict: PRODUCTIVE.

- Round 2 pass 19 (safe-url-range-cache, mission 19), commit ec86e27, src/html.rs only (+63/-3). Lever: safe_url_cached hits previously memoized ONLY the boolean and re-ran safe_url(url, context) to recover the trimmed slice — every hit re-paid trim_matches (a CHAR-class trim incl. multi-byte C0/C1 controls) + the full url_scheme byte scan. UrlCacheEntry now also carries (trim_start, trim_end) derived ONCE at miss time by pointer-differencing the exact slice safe_url returned against url (usize casts only — safe Rust, no unsafe, zero re-implementation of the trim class, so it cannot drift); hits return url.get(start..end) directly. Cache-key audit (mission ask): key is (UrlContext, raw_url) — safe_url's ENTIRE input tuple (pure function, no ambient state) — already encodes everything; NO key change needed. Consumer audit: html.rs safe_url_cached is the ONLY cached consumer (Inline::Link href + Inline::Image src); pdf.rs safe_pdf_link is a separate UNcached implementation (mailto/tel allowlist, owned LinkTarget) — candidate for its own mission; diff.rs is_safe_href is a boolean re-impl for diff HTML; pdfa.rs uri_forbidden_in_pdfa is a compliance gate. Unit test added: url_cache_hit_re_slices_the_exact_safe_url_trim_range (11 edge cases incl. U+0085/U+009C multi-byte trims, empty, whitespace-only, both contexts; miss/hit/third-lookup == fresh safe_url ground truth). Equality: before/after binaries from frozen worktrees @ d176c2c (differ ONLY in src/html.rs; RCH_SHIM_LOCAL_IDE=1 + isolated per-tree CARGO_TARGET_DIR; md5 d95f1458…/7feedaaa…, mtime-verified fresh) — ALL 10 deterministic artifacts byte-identical (--scenario all --iters 25); 5 telemetry jsonl differ in *_ns fields only (jq-audited: 50/50 records identical after stripping *_ns keys); golden_output 1/1; cargo test --lib 580/580; clippy --lib release-perf exit 0 zero warnings; determinism ok; wasm-core ok (frozen after-worktree). Perf: ambient-immune standalone-rustc direct-lever micro (both cache shapes + real safe_url family verbatim, interleaved w/ order rotation, equality asserted): hit-dominated html-large shape (3 distinct x70) -82.3..-84.4%, wide 24-distinct -48.1..-51.4%, miss-path +3.7..+4.0% (one-time per distinct URL). End-to-end paired 5x200 (load 29-33): stock html-large -0.26% p50-med / html-showcase +0.52% — honestly sub-noise, as predicted (safe_url ~1% self there); the consumer-that-matters surface, a 419,878 B link-dense corpus (6,000 links / 8 distinct dests, ~99.9% hits), corpus-html p50-med 512,917->477,459 ns = -6.91%, p95-med 660,375->615,042 ns = -6.86%, after-faster 4/5. Proof: tests/artifacts/perf/round2-pass19-safe-url-range-cache/OPTIMIZATION_PROOF.md (check-optimization-proof.sh ok); bench-interleaved-5rounds.tsv + bench-corpus-html-5rounds.tsv + micro-direct-lever.txt + pass19_micro.rs + golden_checksums_{before,after}.txt + 6000-link-corpus.md + mission.patch (rollback artifact). Verdict: PRODUCTIVE (hit path deletes a full trim+scan per repeated link/image destination; decisive on any link-recurring document, -82% per-hit micro, -6.9% end-to-end on link-dense HTML render).

- Round 2 pass 20 (math-cache-lru, mission 20), commit 53a72b3, src/html.rs only (+151/-25). Lever: every Inline::Math / Inline::DisplayMath / Block::MathBlock occurrence re-ran fmd_math::parse(tex.trim()) + to_mathml(&node, display) from scratch — no cache. RenderState now carries math_cache, a bounded 16-entry ring (eviction mechanics copied verbatim from highlight_cache: fill-to-N then next-pointer rotation) keyed (tex.trim(), display) -> MathML String; all three call sites route through push_mathml_cached, hits push the memoized bytes by borrow (no clone, same hit discipline as highlight_cache). Purity audit (proof doc): parse's only ambient state is an idempotent OnceLock<MacroSet::new()> constant (repo-wide grep for static mut/atomics/thread_local/RefCell/Cell/LazyLock/OnceLock/once_cell/lazy_static in fmd-math/src finds only that site; \newcommand expansion is source-driven), to_mathml reads only (node, display) — so MathML is a pure function of the exact cache key; MathBlock and DisplayMath legitimately share key (tex.trim(), true) since both pushed to_mathml(node, true) verbatim; Err paths are NOT cached (fallback embeds UNtrimmed tex, varies beyond key, and is a cheap escape). Insert-only-on-miss keeps keys unique so first-match find is deterministic. Results: math-heavy corpus (191,816 B, 3,240 spans, 1,710 hot occurrences, 14 distinct keys) 5x200 paired interleaved: corpus-html p50 median 3,441,541 -> 407,541 ns = -88.16% (p95 3,957,375 -> 598,375 = -84.88%), after-faster 5/5; controls corpus-parse +0.14% / corpus-pdf -0.46% (noise); stock controls sub-noise (html-showcase -11.45% median is ambient: per-round spread -18.2..+12.7%, showcase.md has zero math). Direct-lever micro (pass20_micro.rs, cargo example in the frozen after-tree linking REAL fmd_math, interleaved + order-rotated, 39,952 byte-equality assertions vs fresh ground truth on BOTH shapes): corpus-shape -98.2..-98.3%, hot-only x2000 -98.9..-99.0%, miss-path 24-distinct +4.9..+6.4% (once per distinct key). Equality: before = frozen tree @ 508b747 + mission.patch reverse-applied (diff -rq: trees differ ONLY in src/html.rs; harness md5s eb9d7d71…/f09352d7…, mtime-fresh 1788037628, RCH_SHIM_LOCAL_IDE=1 + isolated per-tree CARGO_TARGET_DIR evading the rch stale-example bug); ALL 10 deterministic artifacts byte-identical (--scenario all --iters 25), 5 telemetry jsonl differ in *_ns only (50/50 records identical after strip), AND math-corpus goldens corpus.html (955,406 B) + corpus.pdf byte-identical (cmp) — equality proven on the changed surface itself; golden_output 1/1; cargo test --lib 582/582/5-ignored incl. 2 new tests (key exactness: trim-equality, display-flag separation, hit==fresh ground truth, Err-not-cached; ring cap at 16 + end-to-end repeated-formula determinism); clippy --lib release-perf exit 0 zero warnings; determinism ok; wasm-core ok (all four checks, frozen after-tree). Proof: tests/artifacts/perf/round2-pass20-math-cache-lru/OPTIMIZATION_PROOF.md (check-optimization-proof.sh ok); bench-corpus-5rounds.tsv + bench-stock-5rounds.tsv + micro-direct-lever-raw.txt + bench-summary.txt + golden_checksums_{before,after}.txt + mission.patch (rollback artifact) + math-heavy-corpus.md + pass20_micro.rs + run-corpus-bench.sh/run-stock-bench.sh. Verdict: PRODUCTIVE (-88% end-to-end on math-bearing HTML render, 5/5; -98..-99% at the lever; byte-identical output everywhere)

- Round 2 pass 21 (dragon-float-fixed-format, mission 21), src/diagrams.rs only (+345/−57, swept into peer batch commit 9c088ae mid-flight; verified present at HEAD; this pass's own commit is the ledger entry only). Lever: every emitted SVG coordinate/size went through std `{:.1}`/`{:.0}` → float_to_decimal_common_exact → dragon4 format_exact (baseline: dragon.rs 1.8% self / ≈3.4% combined in html-large, ~24k formats/iter from ~254 showcase.md flowchart copies; passes 17-20 shrank the rest so the share grew). New private `write_svg_fixed(out, v, frac_digits)`: f32 decomposed to normal m·2^e2, and since value·10^N = m·5^N/2^(k−N) exactly (k=−e2), the rounded digit string + round-half-to-even tie test are ONE integer shift + ONE bitmask compare (e2≥0 is the integer m<<e2); digits spliced via one write_str through zero-alloc Display wrappers SvgNum ({:.1}) / SvgInt ({:.0}); all 22 sites in render_flowchart/render_sequence_diagram/render_ascii_diagram converted (templates byte-identical, only {:.N}→{} + arg wrap); |v|<0.05 prints the zero form (no exact 0.05 in binary, boundary never a tie); NaN/inf/|v|>4e9 fall back to std format! (unreachable from geometry). Reachable half-even ties are real (label width 8.5·len+32 → e.g. total_width 250.5 → "250") and locked by test. diff.rs/doc_stats.rs f64 sites left on std by design (~12 values/doc, per-document stats — documented, not worth the f64 proof).
- Results: direct lever (in-process interleaved micro on the exact enumerated call-site population, 2,492 geometry values, same String allocs both sides): 136.4 → 33.0 ns/float p50 = −75.8% (p10 −75.9/p90 −76.1), raw writer 14.2 ns/float. End-to-end paired 5×200 (frozen b91836d binaries, order alternating, load 19-126): html-large median p50 5,661,084 → 4,880,416 ns = −13.79% after-faster 5/5 (rounds −8.85..−22.44%); diagram-dense 900-flowchart corpus corpus-html 21,073,375 → 13,372,834 ns = −36.54% 5/5; corpus-pdf −1.56% (3/5, near noise); html-showcase +1.97% median 1/5 — honestly sub-noise (one flowchart ≈ 94 formats ≈ 10µs ≈ 1.7%, rounds swing ±19%).
- Equality (isomorphism proof = differential test): 18,330,736 value-format comparisons vs format!, 0 mismatches (every exponent class × 8192 mantissas × both signs incl. subnormals/NaN/inf fallbacks; dyadic j/2^k k≤14 all-tie grid; 3M random bit patterns; full realistic population) — release-mode standalone (pass21_micro.rs, micro-direct-lever-raw.txt); committed unit tests (7 new: dyadic grid, integers+cap sweep incl. Display wrappers, all-exponent sweep, random bits, std-probe pins, end-to-end half-even viewBox) keep it green in CI. Before/after binaries (b91836d ± only src/diagrams.rs, RCH_SHIM_LOCAL_IDE=1 + isolated CARGO_TARGET_DIR, md5 37ff71b4…/d75bffb9…): ALL 10 deterministic artifacts byte-identical on --scenario all --iters 25; 5 telemetry jsonl differ in *_ns only (50/50 records audited); diagram-dense corpus corpus.html (3,433,923 B)/corpus.pdf byte-identical (cmp). golden_output 1/1; --lib 584/584 at HEAD; clippy exit 0 zero warnings in the isolated after-tree (HEAD clippy currently fails E0658 in PEER fee561e/671f866 const-fn code — absent at b91836d and in my isolated tree; reported to Main, untouched); determinism ok; wasm-core ok.
- Proof: tests/artifacts/perf/round2-pass21-dragon-float-fixed-format/OPTIMIZATION_PROOF.md (check-optimization-proof.sh ok); bench-paired-5rounds.tsv + micro-direct-lever-raw.txt + pass21_micro.rs + diagram-dense-corpus.md + golden_checksums_{before,after}.txt + mission.patch (545-line rollback artifact)
- Verdict: PRODUCTIVE (−75.8% per formatted coordinate; html-large −13.79% 5/5; diagram-dense HTML render −36.54% 5/5; dragon4 no longer on the HTML path; bytes identical everywhere)

- Round 2 pass 22 (footnote-walk-gating, mission 22), src/html.rs only (+28/−10). Lever: render/render_fragment walked the ENTIRE block tree per render collecting footnote definitions (13,213 block-node visits per html-large render, returning an empty Vec — footnote-free docs are the norm) before any rendering. RenderState now carries root_blocks: &'a [Block] + footnote_defs_scanned; footnote_defs is filled by ensure_footnote_defs(), invoked as the first statement of render_footnote_ref — the FIRST Inline::FootnoteRef actually rendered runs the unchanged walk exactly once (lazy-trigger, one step lazier than round-1's needs_toc pre-scan gate because a block-tree pre-scan costs the traversal it guards). Reader audit in the proof: footnote_defs has exactly two readers, both ref-gated (render_footnote_ref's has_def — now triggers the idempotent collection first; push_footnotes_section — early-returns while footnote_order is empty, and footnote_order only grows inside render_footnote_ref), so deferral is output-exact; render_fragment keeps its own-blocks walk scope (orphan refs stay literal), collect_toc_entries' throwaway default state never scans.
- Results: direct lever (ambient-immune in-process interleaved micro, walk duplicated verbatim, 30 windows, harness-identical 1 MiB generator): walk 19,485 ns/render p50 (p10 18,067/p90 23,936) -> gated 2 ns p50 = 99.99% eliminated; ensure guard 0.3 ns/call when refs exist. End-to-end harness html-large paired 200-iter interleaved: 3,700,562 -> 3,646,292 ns median over 10 rounds = −1.47% (5/10; first set −2.86% 4/5, second flatter at load 26-40); parser-large untouched-path control +0.11% (3/5, band −0.51..+3.22) bounds ambient; html-showcase +2.92% (2/5) honestly sub-noise (walk share ≈0.3% max); deterministic component = micro's 13.5-19.5 µs ≈ 0.4% of the 3.7 ms render. Footnote-bearing corpus (16 defs, all shapes): render paired median −4.85% (4/5) — ≥ parity as predicted (work-neutral when refs exist).
- Equality: before/after binaries from frozen worktree at 42d09af differing ONLY in src/html.rs (RCH_SHIM_LOCAL_IDE=1 + isolated CARGO_TARGET_DIR; md5 cb9cd249…/f7ba30ea…): --scenario all --iters 25 → ALL 10 deterministic artifacts byte-identical; 5 telemetry jsonl differ in *_ns only (50/50 records python-audited); footnote-heavy corpus (duplicate defs, defs in lists/quotes, nested defs, unicode id, unreferenced defs, orphan refs, notes-citing-notes, [[TOC]] interplay) renders corpus.html (58,742 B) + corpus.pdf byte-identical; render_fragment paths byte-identical across 5 cases (pass22_fragment_check.rs both lib states). golden_output 1/1; --lib footnote 13/13; search_index_test (render_fragment consumers) 14/14; determinism ok; wasm-core ok; clippy --lib release-perf exit 0 (3 warnings all pre-existing parse/mod.rs lints, zero in html.rs).
- Harness finding (pre-existing, reported, not touched): --scenario corpus aborts with "profiled pdf-large render bytes differed from normal render bytes" for ANY footnote-bearing input, reproducible identically on both binaries (minimal 2-line footnote doc fails, no-footnote control clean); goldens are written before the abort and were compared byte-identical.
- Proof: tests/artifacts/perf/round2-pass22-footnote-walk-gating/OPTIMIZATION_PROOF.md (check-optimization-proof.sh ok); micro-direct-lever-raw.txt + pass22_micro.rs + bench-paired-5rounds.tsv + bench-extra5-control.tsv + fragment-check-{before,after}.txt + footnote-heavy-corpus.md + golden_checksums_{before,after}.txt + mission.patch (81-line rollback artifact)
- Verdict: PRODUCTIVE on footnote-free documents (walk deleted outright — 13-20 µs/render on 1 MiB docs, 99.99% of the lever's cost; html-large −1.47% median 10 rounds at extreme ambient) and EXACTLY NEUTRAL on footnote-bearing ones (same single walk deferred; byte-identical everywhere)
- Commit: source change swept into peer batch commit 2ea617b mid-flight (verified present intact at HEAD: all six hunks, zero eager collect_footnote_defs construction sites, golden_output 1/1 re-verified post-sweep; rollback = mission.patch, reverse-apply checked). This pass's own commit carries the ledger entry only (pass-21 precedent); peers' newer uncommitted html.rs url_scheme edits left untouched/unstaged

### Pass 23 — inline-cache-admission — 2026-08-29T18:55
- Files changed: src/parse/mod.rs only (+87/−3, own clean commit 65c6801): the per-document InlineParseCache now admits a text only on its SECOND sighting. Every unique cacheable paragraph previously paid the insert-side deep clone (key to_string + inlines.to_vec()) for a hit that never comes — reference-link-heavy pays 512/parse (capped) for exactly 0 hits. First sightings are tracked as bounded FNV-1a-64 hashes in a flat Vec<u64> on the cache (seen_hashes, cap INLINE_PARSE_CACHE_MAX_TRACKED_HASHES=512 entries = 4 KiB L1-resident, cleared wholesale at capacity so late repeats re-qualify after a fresh pair); parse_inlines_with_refs_profiled inserts only when inline_cache.admit(text). Second sighting still parses (returning the first tree AND keeping a copy costs the same clone the policy deletes), so the earliest hit moves 2nd→3rd sighting: per distinct repeated text +1 parse, per unique −1 insert deep clone. Admission hashes can't corrupt results: entries stays keyed by exact String; collisions only admit early (one wasted insert)
- Mission's removal question answered with data: cache stays. Origin f8140ba (2026-07-08) added it for parser-large's repeated seed paragraphs (inline_parse attribution 49.98→41.39ms/5 parses). Today's parser-large telemetry (this pass's before-binary) shows exactly ONE distinct cacheable text repeating (telemetry delta: +1 inline_parse invocation per parse, count 465,200→465,225 over 25 iters) — two-hit keeps that hit stream while deleting the ~512 unique-clone inserts, strictly better than removal (−1.12%, 9/10) which would forfeit the hits
- Results: direct lever (pass23_micro.rs, standalone rustc -O --edition 2024, both policies verbatim, 30 interleaved windows/stream): unique-heavy (reference-link-heavy shape, 800×1) cache-side p50 212,167→76,584 ns = −63.9% (265.21→95.73 ns/sighting; 800 insert deep-clones → 0); repeat-heavy parser-large shape (48×65) −1.6%; table-cell shape (1×1920) −1.7%; worst-case tracker-at-cap scan 143.6 ns/sighting; stored-value invariant equal every stream. End-to-end corpus classes (fmd_parser_perf, 10 interleaved rounds × 100 iters, order alternating, all 7 output checksums identical both binaries): reference-link-heavy −14.62% 10/10; code-fence-heavy −7.27% 9/10; readme −3.65% 9/10; parser-large-1mib −1.12% 9/10; showcase −5.44% 8/10 (r1-5 +7.6% inverts to −6.9% in r6-10 — ambient at 23 µs scale); table-heavy +0.71% 4/10, mechanically bounded by one extra 25-byte-cell parse (~+0.1%). Harness paired 5×200: parser-large medians 6,345,908→6,311,033 ns = −0.55% (3/5), html-showcase 491,833→466,075 = −5.24% (4/5)
- Equality: before/after frozen worktrees at 6702ad5 differing ONLY in src/parse/mod.rs (diff -rq; RCH_SHIM_LOCAL_IDE=1 + isolated CARGO_TARGET_DIR; harness md5 12d52006…/1dc804a6… after blank-line lint fix + rebuild + re-verify). ALL 10 deterministic artifacts byte-identical on --scenario all --iters 25; 5 telemetry jsonl: non-ns fields record-for-record identical EXCEPT the one inline_parse stage record whose work counters honestly shift by the second-sighting parse (+1 invocation/+35 chars/+7 allocations per parse; pdf-* fully identical); parser_perf proof_obligation ast_and_html_equivalence=pass; golden_output 1/1; lib 727/727 (84+54+589, incl. new inline_parse_cache_admits_only_on_second_sighting); parser_conformance repeated_inline + 6 profiled_parser pass; determinism ok; wasm-core ok; clippy --lib --profile release-perf 0 new warnings
- Proof: tests/artifacts/perf/round2-pass23-inline-cache-admission/OPTIMIZATION_PROOF.md (check-optimization-proof.sh ok); micro-direct-lever-raw.txt + pass23_micro.rs + run-paired-bench.sh + run-parser-perf-bench.sh + bench-paired-5rounds.tsv + bench-parser-perf-{5rounds,extra5rounds}.tsv + golden_checksums_{before,after}.txt + mission.patch (139-line rollback artifact)
- Commit: 65c6801 (own commit, clean, single file; peer ios/ edit untouched/unstaged)
- Verdict: PRODUCTIVE (−63.9% cache-side on unique-heavy streams; −14.62% end-to-end reference-link-heavy 10/10; every corpus class non-regressing with byte-identical output everywhere; repeat-heavy pays at most one parse per distinct repeated text, measured ≤ noise)

### Pass 24 — inlines-from-chars-entry — 2026-08-29T19:00
- Files changed: src/parse/mod.rs only (+178/−38 incl. new unit test, own clean commit 34ae4ee): nested inline content (inline-link text, reference-link text, strikethrough inner) was collected into a temporary String (chars[a..b].iter().collect()) and re-parsed through the &str entry, which re-decoded it into a fresh Vec<char> (collect_inline_chars_from_text) before the char-based core — one UTF-8 encode + one decode + two allocations per nested span, plus a text.to_string() clone on the plain fast path, plus the link-text collect even for candidates that later failed destination validation. New parse_inlines_chars_nested(&[char], refs, profiler): char-slice nested entry that gates via inline_chars_needs_full_parse (char-slice mirror of inline_text_needs_full_parse; equivalence: every matched special is ASCII so byte-eq and char-eq agree, and the :///www windows are pure ASCII comparisons where byte-window == char-window bijectively) and hands the parent buffer slice straight to parse_inlines_chars_with_refs_profiled, whose signature moved Vec<char> → &[char] (both pre-existing callers adapted). parse_delim returns the inner char range (start, end, next) instead of a collected String. AST-stored Strings untouched (code spans, math TeX, footnote ids, link dest/title, autolink labels). Cache admission stays depth-0-only (nested entry never consults/inserts; depth inc/dec replicated around its work); profiler ledger parity kept (byte_len computed as sum of len_utf8 only when enabled — record_since no-ops when disabled, so telemetry counts stay byte-equal)
- Results: direct lever (pass24_micro.rs, standalone rustc -O, both sides mirroring the exact deleted/added entry code verbatim, 30 interleaved windows/class, 4800-span gate-equivalence audit passed): plain-reflink spans (reference-link-heavy shape) 48→31 ns/span = −35%; plain-showcase 45→35 = −22%; full-parse gate-true spans 109→11 = −89%; unicode-mixed 107→5 = −95%; scheme-gate 97→6 = −93%. End-to-end (fmd_parser_perf, 5 interleaved rounds × 100 iters, order alternating, all 7 output checksums identical every round both binaries): reference-link-heavy 1,267,467→1,183,858 ns = −6.59% (after-faster 5/5); readme −4.84%; showcase −5.71%; malformed-diagnostics −5.58%; code-fence-heavy −0.90%; table-heavy −0.15%; parser-large-1mib +0.25%. Harness parser-large paired 10×200: bands fully overlap under a +13% ambient ramp (before 6,215,292..7,119,583 / after 6,378,875..7,422,292 p50); ambient control = 16 tight back-to-back 40-iter alternations: median pair delta −1.59%, mean −0.02%, after-faster 9/16 → neutral-within-noise, as expected (parser-large's nested-span population is a small share of its 6.5 ms)
- Equality: before = frozen worktree at 92c7d4f, after = working tree, differing ONLY in src/parse/mod.rs (RCH_SHIM_LOCAL_IDE=1 + isolated CARGO_TARGET_DIR; harness md5 2b3b6ab7ac0510074df148921971b704 / d49b1feab6f73da1b0b05651a2f38419 — the post-clippy borrow-elision rebuild produced a bit-identical after-binary, freshness verified by mtime). --scenario all --iters 25 → ALL 10 deterministic artifacts byte-identical; all 5 telemetry jsonl identical modulo *_ns (stage count/work_count/allocation_count/input_bytes byte-equal). golden_output 1/1; --lib parse:: 97/97 incl. new char_slice_gate_matches_byte_gate_exactly (battery + every substring, multibyte-at-boundary traps); determinism ok; wasm-core ok; clippy --lib --profile release-perf 0 warnings (22 transient needless_borrow from the Vec→&[char] move fixed in-tree before the final build)
- Pre-existing, NOT this pass (repro'd identically on the frozen before-worktree at base 92c7d4f, 108 passed/1 failed both sides): parser_conformance four_space_reference_title_line_is_not_consumed — fallout of peer commit 6702ad5 (multi-space reference-title continuation), reported for main-agent triage
- Proof: tests/artifacts/perf/round2-pass24-inlines-from-chars-entry/OPTIMIZATION_PROOF.md (check-optimization-proof.sh ok); micro-direct-lever-raw.txt + pass24_micro.rs + run-paired-bench.sh + run-parser-perf-bench.sh + bench-paired-5rounds.tsv + bench-parser-perf-{5rounds,checksums}.tsv + parser-large-tight16pairs.txt + golden_checksums_{before,after}.txt + mission.patch (414-line rollback artifact)
- Commit: 34ae4ee (own commit, clean, single file; peer ios/ edit untouched/unstaged)
- Verdict: PRODUCTIVE (−35%..−95% per nested span on the entry lever; reference-link-heavy −6.59% 5/5, readme/showcase −5%; parser-large honestly neutral-within-noise with the tight-pair control; bytes identical everywhere)

### Pass 25 — bracket-pair-u32 — 2026-08-29T19:45
- Files changed: src/parse/mod.rs only (+180/−13 vs 752cf0f, swept into peer commit d6031cd — see Commit; this pass's own commit carries the ledger entry only, pass-22 precedent): compute_bracket_pairs's Vec<Option<usize>> bracket-pair table (16 B/char, vec![None; n] fill, unpresized open_stack) → BracketPairs: Compact(Vec<u32>) with u32::MAX sentinel (4 B/char) behind the domain guard chars.len() <= u32::MAX (pass-5 discipline; >2^32-char library inputs keep the Wide(Vec<Option<usize>>) full-width fallback; wasm32 = tautologically Compact; sentinel can't collide — max real index is u32::MAX−1 at the boundary). The pairing loop lives ONCE in compute_bracket_pairs_in::<S: PairTable> (CompactTable/WideTable = pure storage), so pairing decisions are identical by construction; consumers (parse_link_like, parse_reference_link_like) swap .get().copied().flatten() → partner() (value-for-value identical incl. OOB→None). open_stack presized len/64 (capacity-only) — picked from data: measured peak depth is 1 on every realistic corpus shape (≤63 opens/1000 chars), len/64 neutral-to-better (−3.4..+1.7% sub-noise) while the bracket-count pre-pass alternative measured +9..+24% SLOWER (extra scan > realloc savings), rejected.
- Results: direct lever (pass25_micro.rs, standalone rustc -O, both sides verbatim, 30 interleaved windows/stream, 131,363-index differential equality pin incl. one-past-end): per-compute −7.8..−22.7% on all six streams (reflink-800 −14.0%, inline-800 −8.6%, deep-nest-64 −18.6%, prose-2k −7.8%, deg-open-4k −22.7%, unicode-mixed −18.2%); table bytes 16→4/char (inline-800: 753→188 KB/call); data-bearing lookup streams −18..−40% (reflink −39.6%); two sentinel-only streams +10/+19% at 0.35-0.42 ns/query (sub-ns branch-microstructure, reported as-is). End-to-end harness parser-large paired 5×200 interleaved (order alternating): p50 6,682,975→6,472,883 = −3.15% and p95 7,825,017→7,367,058 = −5.85%, after-faster 5/5 on BOTH metrics (p50 −0.89..−4.51%, p95 −3.62..−8.20%). Link-heavy fmd_parser_perf 5×200 (checksums identical every round both binaries): reference-link-heavy −3.79% (4/5), showcase −5.34%, malformed-diagnostics −4.92%, code-fence-heavy −2.56%; table-heavy +0.50%/parser-large-1mib +1.12%/readme +2.55% sub-noise (readme flipped sign vs the 25-iter control run — ±2-3% ambient band).
- Equality: before = clean 752cf0f, after = frozen worktree /tmp/fmd-p25-after-tree = 752cf0f + mission.patch ONLY (diff -stat 1 file +180/−13; RCH_SHIM_LOCAL_IDE=1 + isolated CARGO_TARGET_DIR, md5 3fc2f85c…/9be25fa7…, freshness mtime-verified). --scenario all --iters 25 → ALL 10 deterministic artifacts byte-identical; 5 telemetry jsonl differ *_ns-only (451/451 non-ns fields equal; before-binary control run reproduces the identical 5-file nondeterminism). NOTE: html-large/parser-large checksums differ from round2-baseline (e922…→a406…) — pre-existing drift from peer conformance commit 6702ad5, present in BOTH binaries of this pair; the pass's required before-vs-after control is byte-identical. golden_output 1/1; --lib parse:: 98/98 incl. new bracket_pair_representations_agree_and_route_compact (17-string battery, per-index Compact≡Wide≡routed incl. OOB); determinism ok; wasm-core ok; clippy --lib --profile release-perf 0 warnings.
- Pre-existing, NOT this pass: parser_conformance four_space_reference_title_line_is_not_consumed 108/1 on the after-worktree — 6702ad5 fallout (ancestor of before-tree), orthogonal block-level scanner test, untouched by this representation-only diff.
- Proof: tests/artifacts/perf/round2-pass25-bracket-pair-u32/OPTIMIZATION_PROOF.md (check-optimization-proof.sh ok); micro-direct-lever-raw.txt + pass25_micro.rs + run-paired-bench.sh + run-parser-perf-bench.sh + bench-paired-5rounds.tsv + bench-parser-perf-{5rounds,checksums}.tsv + golden_checksums_{before,after}.txt + binary-ids.txt + mission.patch (273-line rollback artifact, reverse-apply --check verified on the frozen after-worktree)
- Commit: source change swept into peer conformance commit d6031cd (peer added code-span/inline-HTML skip arms INSIDE the shared pairing loop minutes after this pass's edit landed; coexists composing — live HEAD e2519eb cargo check --lib clean, bracket tests 10/10). This pass's own commit: ledger entry only. Rollback on live HEAD needs --3way/manual hunk selection (peer arms sit inside the same function); the verified-clean surface is the frozen pair.
- Verdict: PRODUCTIVE (−8..−23% per bracket-pair compute with 4× smaller table; parser-large p95 −5.85% 5/5, p50 −3.15% 5/5; reference-link-heavy −3.79% 4/5; bytes identical everywhere; honest sub-noise on 1 MiB mixed corpora)

### Pass 26 — code-span-no-alloc — 2026-08-29T20:05
- Files changed: src/parse/mod.rs only (+112/−7 vs 03035b3, landed via peer-swept commit c882799, verified bit-identical to the frozen after-worktree source; this pass's own commit carries the ledger entry): every inline code span paid 2-3 heap allocations — caller's chars→String collect, then normalize_code_span(&str)'s unconditional `s.replace('\n', " ")` (std replace allocates a full-length String even when no '\n' matches) plus a third `s[1..len-1].to_string()` when the strip guard fired. New normalize_code_span(&[char]): fused strip guard (first/last char ' '|'\\n' — replace maps '\\n'→' ' 1 byte→1 byte so the old byte-strip equals this char-strip and is_whitespace classes are preserved, making old `trim() != ""` == `any(!is_whitespace)`; len>=2 implied, kept for slice safety) + ONE collect straight from the char slice — clean spans zero extra passes, newline spans fold '\\n'→' ' during the collect (byte-identical to replace: one space PER newline, runs never collapsed), strip shapes slice the char range before collecting. Per-span allocations: clean 2→1, newline 2→1, strip 3→1, newline+strip 3→1
- Results: direct lever (pass26_micro.rs, standalone rustc -O, both sides verbatim, 30 interleaved windows/stream): clean-typical −42.6% (38.4→22.1 ns/span, 30/30); clean-long −24.4%; newline-bearing −44.2%; strip-needed −62.2%; newline+strip −64.9%; unicode-mixed −45.8%; mixed-realistic-800 −45.0%; corpus-extracted (every single-line span of the end-to-end corpus, maximal-run matched) 49.5→32.9 = −33.5% (28/30). End-to-end code-span-heavy corpus (791,267 B, ~23.5k spans, harness --scenario corpus paired 5×50 interleaved, order alternating): corpus-parse 7,690,708→6,493,083 ns median = −15.6%, after-faster 4/5 rounds; corpus-html −0.8% (control, parse not on path); corpus-pdf +8.2% median with ±15% per-round swings (parse-independent control bounds ambient); all 20 corpus.html/corpus.pdf outputs byte-identical across binaries×rounds (corpus.html sha256 198200a0…, corpus.pdf 8a91530d…)
- parser-large paired 5×200 (contract step): honestly sub-noise BY CONSTRUCTION — parser-large = showcase.md ×256 and showcase.md carries only 4 non-fence backticks → ~512 inline code spans in 1 MiB → expected ≈ 512 × 17 ns ≈ 0.1% of the 7.2-8.5 ms parse; measured +4.6% median, after-faster 2/5, with one ambient round at +37% (p95 15.2 ms) — unmeasurable under this session's ambient; the decisive surfaces are the corpus bench + micro (context: direct-lever micro decisive when sub-noise)
- Equality: before = frozen worktree at 03035b3, after = frozen worktree 03035b3 + mission.patch ONLY (diff -stat 1 file +112/−7; RCH_SHIM_LOCAL_IDE=1 + isolated CARGO_TARGET_DIR; harness md5 906a3dee…/73d72ee0…, freshness mtime-verified). --scenario all --iters 25 → ALL 10 deterministic artifacts byte-identical; all 5 telemetry jsonl record-for-record identical modulo *_ns (every non-ns field incl. counts byte-equal, 50/50 records python-audited). golden_output 1/1; --lib parse:: 100/100 (98 prior + 2 new: oracle differential normalize_code_span_matches_replace_semantics_across_all_shapes incl. 19,531-string exhaustive 5^0..5^6 sweep over {a,' ','\\n',é,\\t} + end-to-end shapes test); parser_conformance 109/109 (the KNOWN 6702ad5 four_space regression was fixed by peer e2519eb before this pass's before-tree); clippy --lib --profile release-perf 0 warnings; determinism ok; wasm-core ok; mission.patch reverse+forward apply --check verified on the frozen pair
- Differential pin: 260,585 before/after comparisons vs the old implementation kept verbatim as oracle (41,054 population spans incl. every corpus span + 219,531 sweep strings: exhaustive small-alphabet + 200k fixed-seed random over NBSP/CR/emoji/CJK) — 0 mismatches
- Proof: tests/artifacts/perf/round2-pass26-code-span-no-alloc/OPTIMIZATION_PROOF.md (check-optimization-proof.sh ok); micro-direct-lever-raw.txt + pass26_micro.rs + code-span-heavy-corpus.md + run-corpus-bench.sh + run-paired-bench.sh + bench-{corpus,paired}-5rounds.tsv + golden_checksums_{before,after}.txt + golden-{before,after}/ + harness-{before,after}.jsonl + binary-ids.txt + mission.patch (119-line rollback artifact, apply --check both directions)
- Commit: source swept into peer commit c882799 (verified bit-identical: `git show c882799:src/parse/mod.rs` diffs empty vs the measured after-tree source); this pass's own commit: ledger entry only (pass-22/25 precedent)
- Verdict: PRODUCTIVE (one allocation+memcpy deleted per code span on every shape, two on strip shapes; −24..−65% per span across all shapes in the micro; −15.6% median corpus-parse on a 23.5k-span corpus 4/5 under extreme ambient; parser-large honestly sub-noise at ~0.1% expected share; bytes identical everywhere)

### Pass 27 — code-body-presize — 2026-08-29T20:30
- Files changed: src/parse/mod.rs only (+23/−2 vs ade2e6e, own clean commit 5565c1b; committed source verified bit-identical to the measured frozen after-worktree source): (a) fenced-code body collection (scanned_open_fence arm of parse_blocks_with_refs_profiled) — prescan walks the same lines from the same start index with the same pure is_close_fence predicate, summing raw line byte lengths +1 newline each, then String::with_capacity before the untouched push_str/push('\n') collect loop; (b) parse_indented_code — indented_code_extent unchanged, a parallel len-only sum over the computed extent (lines.iter().take(used).map(len+1)) presizes the body String. Both estimates only over-estimate (fence: strip_fence_indent only removes bytes, every line keeps its +1 newline; indented: blank lines push 1 byte counted len+1, content strips ≤4 columns) — push never reallocates mid-collect; capacity is invisible to output bytes.
- Results: direct lever (pass27_micro.rs, standalone rustc -O --edition 2024, both sides verbatim, counting global allocator, 30 interleaved order-alternating windows/stream, differential pin over 11,440 blocks = every body byte-identical + every next-index/extent identical, 0 mismatches): realloc chain → 0 on ALL 8 streams (fence-small 8,868→0, indented-small 8,788→0, mixed 3,991→0/3,895→0, large 462→0/458→0); requested bytes ≈ halve (doubling overshoot gone); ns/block: fence-small −37.7% (30/30), fence-medium −18.8% (30/30), fence-mixed (tilde/inner-backticks/indented-fence/unclosed-EOF/blank-lines) −22.3% (30/30), indented-small −34.3% (30/30), indented-medium −22.1% (30/30), indented-mixed −24.3% (30/30), indented-large −1.7% (22/30); HONEST exception: fence-large-2000line +11.8% (1/30) — at 77 KB/block the prescan's per-line is_close_fence costs more than the realloc chain saves on the standalone harness; real corpora are small/medium-dominated and both end-to-end corpora (which include 12×400-line blocks) still win net.
- End-to-end (fmd_parser_perf 5×200 interleaved, order alternating, all 7 output checksums fnv1a64-identical every round both binaries): code-fence-heavy 139,533→131,308 ns median = −5.9% (clean rounds −13.1/−15.4/−16.5%; the two loss rounds 1&5 show an ambient ramp hitting every scenario incl. untouched paths, readme +18.3% r1); parser-large-1mib −1.5% 4/5. Harness parser-large paired 5×200 (contract): avg p50 6,570,658→6,478,384 = −1.4%, after-faster 4/5, avg p95 7,331,500 equal. Custom corpora (--scenario corpus 5×50 interleaved, deterministic LCG, no footnotes): fence-heavy 531,087 B corpus-parse −7.0% 4/5 (600×3-line + 200×30-line + 12×400-line fences, tilde/indented/info variants); indented-heavy 605,825 B corpus-parse −5.4% 4/5 (1000×4 + 200×30 + 12×400-line blocks, blank continuations); corpus-pdf control −0.6/−1.4% bounds ambient near zero.
- Equality: before/after frozen worktrees at ade2e6e differing ONLY in src/parse/mod.rs (RCH_SHIM_LOCAL_IDE=1 + isolated per-worktree CARGO_TARGET_DIR; harness md5 355da11c…/a56f243a…, parser_perf md5 dcd1011d…/58d88ddc…, freshness mtime-verified against build completion epochs). --scenario all --iters 25 → ALL 10 deterministic artifacts byte-identical; all 5 telemetry jsonl record-for-record identical modulo *_ns + allocation_count (0 residual diff lines after stripping both field families; allocation_count is allocator telemetry the lever legitimately perturbs); all 40 corpus outputs (2 corpora × 2 binaries × 5 rounds × html+pdf) byte-identical. golden_output 1/1; parser_conformance 109/109; --lib parse:: 100/100; clippy --lib --profile release-perf 0 warnings; determinism ok; wasm-core ok (frozen after-worktree).
- Proof: tests/artifacts/perf/round2-pass27-code-body-presize/OPTIMIZATION_PROOF.md (check-optimization-proof.sh ok); micro-direct-lever-raw.txt + pass27_micro.rs + gen_corpora.py + fence-heavy-corpus.md + indented-heavy-corpus.md + run-{paired,parser-perf,corpus}-bench.sh + bench-{paired,parser-perf-5rounds,corpus}-5rounds.tsv + bench-parser-perf-checksums.tsv + golden_checksums_{before,after}.txt + golden-{before,after}/ + harness-{before,after}.jsonl + binary-ids.txt + mission.patch (25-line rollback artifact, apply --check both directions verified)
- Commit: 5565c1b (own commit, clean, single file, not swept; verified bit-identical to the measured after-tree source)
- Verdict: PRODUCTIVE (one allocation family + amortized-growth copies deleted per code block on every shape; reallocs → 0 on all 8 micro streams with bytes ≈ halved; −34..−38% per small block, −19..−24% medium/mixed; code-fence-heavy −5.9% median end-to-end (−13..−16% clean rounds) and both purpose-built corpora −5.4..−7.0% 4/5; one honestly-reported pathological micro shape (fenced 2000-line bodies, +11.8% standalone) that real-document mixes do not hit; bytes identical everywhere)

### Pass 28 — trim-byte-classes — 2026-08-29T20:45
- Files changed: src/parse/mod.rs only (+313/−16 vs a243615, own clean commit; committed source verified bit-identical to the measured frozen after-worktree source): the two profiled trim families in parser-large were std char-pattern scans — trim_space_tab (1.4% self, rank 23) via CharPredicateSearcher (char decode per step), and the str::trim_matches::<char::is_whitespace> monomorphization (1.1% self, rank 27) fed by parse/mod.rs's .trim()/.trim_start()/.trim_end() sites (per-line $$-gate in the block loop, math-block body trims, table-cell trims incl. map(str::trim) on the common path, fence info, depth fallback). All are now byte-class scanners: space/tab trims skip {' ','\t'} bytes directly (1-byte chars, can't appear inside multi-byte sequences ⇒ byte-skip lands on the identical char boundary); new trim_{start,end,}_unicode_ws decide Unicode whitespace from bytes — ASCII via 6-entry table (09–0D, 20), multi-byte by matching the complete 19-sequence non-ASCII White_Space set (C2 85, C2 A0, E1 9A 80, E2 80 80–8A, E2 80 A8/A9/AF, E2 81 9F, E3 80 80; U+200B/U+FEFF deliberately absent and fuzz-pinned) with a backward continuation-walk for trim_end. Ground truth per site FIRST: CommonMark blank/indent/refdef contexts are space/tab-only (class unchanged, mechanism only — incl. normalize_simple_ascii_reference_label closure-trim → trim_space_tab and list_marker trim_start → trim_start_space_tab); $$-gate/math/table/info sites deliberately keep char::is_whitespace semantics (suite pins NBSP-class edges elsewhere), so they got exact-equivalent Unicode scanners, NOT narrowed ASCII classes.
- Results: direct lever (pass28_micro.rs, standalone rustc -O --edition 2024, both sides verbatim, 30 interleaved order-alternating windows/stream×op): differential pin 13,409,568 comparisons / 0 mismatches (every scalar 0..0x10FFFF solo + both edges × 4 fn pairs + full White_Space edge matrix + every corpus string). unicode_start (the per-line $$-gate, hottest converted site): −21..−55% with 30/30 windows on ALL 9 streams. space_tab (the 1.4%-self hotspot): −21..−50%. unicode_full: −4..−54% on 7/9; unicode_end: −8..−54% on 7/9. Honest sub-ns regressions off production paths: bare full-trim +0.27 ns (production full-trims only run on $$-gated lines/table cells, never bare prose), unicode-ws-pad full/end +0.44..0.54 ns (trailing multi-byte ws pays walk+match vs one backwards decode), dollar-gate end +0.63 ns at 15/30 (coin-flip noise, no production site). The exhaustive differential found + forced the fix of one real dev-time bug (backward walk stopped one byte past the lead byte; 2/3-byte trailing ws untrimmed) — net works.
- End-to-end: harness parser-large paired 5×200 interleaved (order alternating; a first run caught a round-5 ambient burst at load-avg 31 and was re-run clean): p50 median 6,221,709→6,100,834 = −1.94% (mean −1.30%, after-faster 5/5; per-round before 6.199–6.244 M vs after 6.086–6.209 M); avg p95 7,002,508 equal both sides (p95 median +2.16% tail noise). fmd_parser_perf all 7 scenarios 5×200: parser-large-1mib −2.56% (5/5), code-fence-heavy −3.05%, malformed-diagnostics −3.97%, showcase −3.80%, readme −2.56%, table-heavy −0.98%, reference-link-heavy −0.49% (4/5 each); output checksums identical every round both binaries.
- Equality: before = clean a243615 frozen worktree, after = a243615 + mission.patch ONLY (diff -stat 1 file +313/−16; RCH_SHIM_LOCAL_IDE=1 + isolated per-worktree CARGO_TARGET_DIR; harness md5 21098a323ded1451b987945fee7230ce/ff0a7561004f81c6800885a13ccefbd1, parser_perf b219eeab31df7bb6c29d3d3fa3b7ce0b/2f14feb6600ab75c8c8e96cfdcf9737f, freshness mtime==build-end-epoch verified both). --scenario all --iters 25 → ALL 10 deterministic artifacts byte-identical (parser-large.html/html-large.html both a406779bc653e928d…, pdf-large.pdf 4103b9dc598d6c3c…); all 5 telemetry jsonl record-for-record identical modulo *_ns ONLY (allocation_count also byte-equal — no allocation change). golden_output 1/1; parser_conformance 109/109; --lib parse:: 104/104 (100 prior + 4 new differential tests incl. every-codepoint sweep + White_Space==25 assertion + 40k fuzz over hostile alphabet); check-determinism.sh ok; check-wasm-core.sh ok.
- Proof: tests/artifacts/perf/round2-pass28-trim-byte-classes/OPTIMIZATION_PROOF.md (check-optimization-proof.sh ok); micro-direct-lever-raw.txt + pass28_micro.rs + run-paired-bench.sh + run-parser-perf-bench.sh + bench-paired-5rounds.tsv + bench-parser-perf-{5rounds,checksums}.tsv + golden_checksums_{before,after}.txt + golden-{before,after}/ + harness-{before,after}.jsonl + binary-ids.txt + mission.patch (431-line rollback artifact, apply --check both directions verified on the frozen pair)
- Commit: a6753a8 (own commit, clean, single file src/parse/mod.rs, not swept; committed source verified bit-identical to the measured frozen after-worktree source)
- Verdict: PRODUCTIVE (the per-line $$-gate wins 21–55% at 30/30 on every stream; the 1.4%-self trim_space_tab hotspot −21..−50%; parser-large p50 −1.94% 5/5 and −2.56% median on the 7-scenario bench; every scenario non-regressing; bytes identical everywhere; Unicode-trim semantics preserved exactly rather than narrowed, pinned by 13.4M differential comparisons)

### Pass 29 — advance-u64-fast-path — 2026-08-29T21:05
- Files changed: src/layout.rs only (+23/−4 vs 696b5c8) + new tests/layout_units_equivalence.rs (differential oracle test, 5 tests). advance_to_layout_units: u128 mul + /1000 + clamp_u128_to_i32 → u64 (exact: advance_1000:u32 × milli_points:u32 ≤ (2^32−1)² = 2^64−2^33+1 < 2^64, NEVER overflows); adjustment_to_layout_units: i128 → i64 (|d·m| ≤ (2^31−1)(2^32−1) < 2^63; signed /1000 truncates toward zero identically); new clamp_u64_to_i32 const fn; clamp_i64_to_i32 already existed (src/layout.rs:3476, 19 call sites) — reused (first attempt duplicated it → E0428, removed). All call-site families untouched (measure_text_with_pairs, measure_advances, CJK pair path, pdf.rs ascii/glyph tables, running-width path). The u128/i128 reference implementations live on VERBATIM as reference_advance/reference_adjustment in the test — the "fallback" is statically unreachable for u32×u32 operands, so it is preserved as the eternal oracle instead of dead code.
- Results: direct lever (pass29_micro.rs, standalone rustc -C opt-level=3 -C codegen-units=1, both sides verbatim, 30 interleaved order-alternating windows, every population equality-pinned before timing): adjustment improved in EVERY population of EVERY interleaved run — cross-run envelope −5.5%..−29.2% ns/call, window wins 18/30..30/30 (archived complete runs: o3 body −11.5% 29/30 / glyph −29.2% 24/30 / random −15.0% 30/30 / clamp-edge −18.0% 22/30; o2 −14.5%..−26.6%, body 30/30); advance neutral-to-positive across runs (−7.8%..+7.2%). HONEST asm finding (asm-before-after.txt): LLVM already canonicalizes the advance u128 form to the IDENTICAL 15-instruction sequence (umull + magic umulh-divide + csel clamp) — that half is zero-cost invariant hardening, not a codegen win; the i128 adjustment form does NOT canonicalize (smulh+mul widening product + adcs/ngcs 128-bit carry-chain clamp checks) — that is the real win.
- Isomorphism proof (mandatory for fixed-point math): tests/layout_units_equivalence.rs — 105,381,808+ comparisons vs the u128/i128 reference, 0 mismatches, debug (overflow checks ACTIVE — the u64/i64 muls can never panic) AND release: 652,536-pair corner cross grid (powers-of-two ±1, extremes, realistic per-mille/mpt values, derived clamp crossings), dense ±2048 clamp windows both signs, every truncation-toward-zero class (products −2999..=2999), 67,633,152 golden-ratio strided full-domain draws (u32 advance × u32 size × signed i32, negatives included), all 65,536 from_points sizes × 512 strided advances, 4M×2 randomized boundary-biased xorshift64* pairs.
- End-to-end: honestly sub-noise under EXTREME ambient load 38–75. Harness paired 5×200 interleaved (bench-paired-5rounds.tsv): paragraph-1k p50 median +3.11% (1/5) — re-ran deep 10×200 (bench-paragraph-1k-deep-10rounds.tsv): −3.70% (7/10); pooled 8/15 = inside the ±8–21% paragraph-1k control envelope prior passes documented on UNTOUCHED code. pdf-large −1.47% (3/5), hyphen-corpus +1.08%, html-large UNTOUCHED control +0.08%. Converters are ~1–2% of scenario self-time → micro deltas predict ≈1% end-to-end, below this environment's floor; the micro is the decisive instrument.
- Equality: before = clean 696b5c8 frozen worktree, after = 696b5c8 + mission.patch ONLY (RCH_SHIM_LOCAL_IDE=1 + isolated per-worktree CARGO_TARGET_DIR on /Volumes/USB_NVME/rch-targets; harness md5 116d759f…/4df24b55…, freshness mtime-verified == build completion epochs). --scenario all --iters 25 → ALL 10 deterministic artifacts byte-identical; all 5 telemetry jsonl record-for-record identical modulo *_ns ONLY (0 residual diff lines after stripping; allocation_count byte-equal on all 50 records). golden_output 1/1; --lib layout:: 25/25; clippy --lib --profile release-perf + --test layout_units_equivalence clean; check-determinism.sh ok; check-wasm-core.sh ok (frozen after-worktree).
- Proof: tests/artifacts/perf/round2-pass29-advance-u64-fast-path/OPTIMIZATION_PROOF.md (check-optimization-proof.sh ok) + micro-direct-lever-raw.txt (both opt-level runs) + pass29_micro.rs + asm-before-after.txt + bench-paired-5rounds.tsv + bench-paragraph-1k-deep-10rounds.tsv + golden_checksums_{before,after}.txt + golden-{before,after}/ + binary-ids.txt + mission.patch (27-line rollback artifact)
- Commit: SWEPT by peer batch-commit: src/layout.rs (+ my tests/layout_units_equivalence.rs) landed inside 74d20e7 (peer bundled it with unrelated parse/conformance work at 20:56); 3ef2291 then touched my test file (div_ceil rewrite of the two ceiling divisions + dead-state cleanup — semantics identical, std API 1.73+, rust-version 1.85 ok). VERIFIED at HEAD: `git show HEAD:src/layout.rs` byte-identical to the measured frozen after-worktree source (so the before/after binary pair measured exactly HEAD's layout.rs state); differential tests re-passed at HEAD 5/5 (debug, overflow checks) + golden_output 1/1. Own commit 6cff84d = ledger entry only (src/test diffs were already committed when I staged). Content hashes recorded here; no revert needed.
- Verdict: PRODUCTIVE on the adjustment lever (−6..−19% per kerning call, mechanism proven at instruction level: widening multiply + 128-bit carry-chain clamp eliminated; 29/30-30/30 windows on 3 of 4 populations), NEUTRAL-BUT-SOUND on advance (asm-identical standalone; guards future type widening and drops reliance on inlined-context range analysis); end-to-end honestly sub-noise at load 38–75; outputs byte-identical everywhere with exhaustive differential pinning.

### Pass 30 — math-metrics-memo — 2026-08-29T22:00 (FINAL PASS — 30 of 30: THE LOOP IS COMPLETE)
- Files changed: fmd-math/src/layout.rs (+44/−5 vs 2536e36) and fmd-math/tests/layout_determinism.rs (+44). Engine gains `glyph_memo: RefCell<Vec<Vec<Option<GlyphMetrics>>>>` — a lazily-grown per-(face, gid) memo of faces::glyph_metrics for the engine's lifetime (roster immutable ⇒ pure; pure even across typeset runs). `metrics_of` = `#[inline]` hit path (RefCell read + two Vec reads) + out-of-line `metrics_of_miss` (font resolve → hmtx/loca/glyf decode → grow-and-store); `space_width` routes its space glyph through the memo. New test `warm_engine_memo_matches_cold_engine_byte_for_byte` pins the memo's contract: warm-memo engine ≡ fresh engine, byte-identical layout_dump+canonical_dump over 4 formulas × warmup-polluted memo. First shape (monolithic metrics_of) was measured SLOWER (+6.1% warm) — the bigger body broke inlining at the five hot glyph sites; the inline hot/out-of-line miss split is what makes the lever positive (failure shape archived, micro-firstshape-6rounds.txt). Vec chosen over HashMap<(face,gid)> (hit = 2 bounds-checked loads; hashing on top of the same RefCell can only lose; measured constraint was codegen, not lookup). Engine becomes !Sync (still Send) — in-repo audit: no static/Sync consumers; public API shape unchanged (G0-3 freeze, private field only).
- Results (direct lever decisive; HONEST scope: the shipped fmd pipeline renders math as MathML strings (pass-20 cache) / PDF mono text and does NOT call Engine::typeset — its consumers are fmd-math tests/examples + external franken_manim Scribe; corpus scenarios ran as the composed no-regression control): p30_micro.rs (examples-form, built identically in both frozen worktrees; 200 DISTINCT formulas, small repeated glyph alphabet; 40 windows × 3 reps × {warm, cold} + parse-only control; 16 order-alternating interleaved process rounds = 640 windows/side/protocol; every formula pinned >5 glyphs + warm==warm byte-identical in-process + before-vs-after layout_dump byte-identical across processes, 323,701 bytes `cmp` clean): warm 4096→3828 ns/typeset = −6.6% trimmed-median, after-faster 11/16 rounds (mean −4.2%), min 3291→3160 = −4.0%; cold 4136→3846 = −7.0%, min −4.6%; parse-only control −3.0% ⇒ implied LAYOUT component −7.8% warm / −8.4% cold (the memo's own surface). Pipeline control (--scenario corpus × math-heavy-corpus.md = 240 distinct formulas so pass-20's 16-entry ring misses, glyphs repeat; 3 interleaved rounds): parse/html/pdf all inside the load-39-56 noise envelope.
- Equality: before = /private/tmp/fmd-p30-before-tree @2536e36, after = @2536e36+mission.patch ONLY (RCH_SHIM_LOCAL_IDE=1 + isolated per-worktree CARGO_TARGET_DIR; harness md5 87e71f9f…/25699b60…, micro f999df53…/3fd94490…, mtimes = build epochs, binary-ids.txt). --scenario all --iters 25 → ALL 10 deterministic artifacts byte-identical (html-large.html a406779b…, pdf-large.pdf 4103b9dc…, font-subset.ttf 39b2e742…, paragraph-1k.breaks 494a41eb… — baseline lineage intact); all 5 telemetry jsonl identical modulo *_ns ONLY (0 residual diff lines; allocation_count byte-equal on every record). cargo test --test golden_output 1/1; cargo test -p fmd-math --features bundled-faces ALL suites ok (layout_determinism 3/3 incl. the new warm/cold pin); clippy -p fmd-math --all-targets clean; check-determinism.sh ok; check-wasm-core.sh ok (std-only RefCell, no unsafe, no deps, wasm32 builds).
- Proof: tests/artifacts/perf/round2-pass30-math-metrics-memo/OPTIMIZATION_PROOF.md (check-optimization-proof.sh ok) + p30-micro-final-raw.txt (16 rounds) + micro-firstshape-6rounds.txt + micro-prerework-8rounds.txt (honest failure/wash shapes) + pass30_micro.rs + math-heavy-corpus.md + bench-corpus-3rounds.tsv + p30-dump-{before,after}.txt + golden_checksums.txt + golden-{before,after}/ + telemetry-{before,after}.jsonl + binary-ids.txt + mission.patch (136-line rollback artifact, apply --check -R verified).
- Commit: source first swept into peer commit ba69d5a (bundled with unrelated conformance work) at the monolithic-shape stage; the final inline-split restructure committed by this pass (see below); mission.patch = `git diff 2536e36 -- fmd-math` of the FINAL shape, so the measured after-binary source ≡ committed source.
- Verdict: PRODUCTIVE on its own surface (−6.6%/−7.0% per parse+typeset call trimmed-median, −7.8%/−8.4% on the isolated layout component, −4.0..−4.6% on minimums, 11/16 rounds; the memo deletes a 4-division hmtx+loca+glyf re-decode per repeated glyph occurrence) with the codegen lesson recorded (memoization that de-inlines hot sites LOSES — +6.1% first shape); bytes identical everywhere; determinism doctrine extended and pinned (warm engine ≡ cold engine).

--- LOOP COMPLETE: 30/30 passes of round 2 delivered. Every pass: one lever, byte-identical outputs, archived proof, checker-validated. ---
