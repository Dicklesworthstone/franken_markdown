# Skill Loop Progress
# Skill: extreme-software-optimization
# Target: /data/projects/franken_markdown
# Total Passes: 15
# Started: 2026-07-05T16:32:00Z

## Status: IN PROGRESS - Pass 6 of 15

## Profiling Handoff
- Artifact directory: `tests/artifacts/perf/optloop-20260705T162726Z`
- Baseline command: `scripts/perf-gauntlet.sh --iters 20 --run-id optloop-20260705T162726Z`
- Top scenarios by p95:
  1. `parser-large`: 1985.255 ms p95, evidence `inprocess.jsonl`
  2. `pdf-large`: 362.162 ms p95, evidence `inprocess.jsonl`
  3. `hyphen-corpus`: 29.779 ms p95, evidence `inprocess.jsonl`
  4. `pdf-showcase`: 11.404 ms p95, evidence `inprocess.jsonl`
  5. `html-showcase`: 0.967 ms p95, evidence `inprocess.jsonl`
- Top parser stage: `reference_collection`, 2000.299 ms p95, evidence `golden/parser-large-stages.jsonl`
- Top PDF stage: `layout`, 264.849 ms p95, evidence `golden/pdf-large-stages.jsonl`
- Behavior oracle: `golden_checksums.txt` plus parser/PDF profiled-vs-normal proof records.
- Perf counter caveat: `perf stat` was blocked by `perf_event_paranoid=4`; no kernel tuning was applied.

## Interim Reprofile After Pass 5
- Artifact directory: `tests/artifacts/perf/optloop-after5-20260705T170300Z`
- Command: `scripts/perf-gauntlet.sh --iters 5 --run-id optloop-after5-20260705T170300Z`
- Hotspot shift: `parser-large` dropped from 1985.255 ms p95 in the original 20-iteration baseline to 71.847 ms p95 in the directional 5-iteration reprofile; `pdf-large` is now the top scenario at 346.178 ms p95.
- Current top PDF stage remains `layout`: 263.273 ms p95, evidence `golden/pdf-large-stages.jsonl`.
- Current parser stage mix: `block_parse_total` 52.668 ms p95, `reference_collection` 22.361 ms p95, and `inline_parse` 5.280 ms p95 per sample, evidence `golden/parser-large-stages.jsonl`.

## Missions
1. Reference consumed-table boundary precompute: eliminate repeated whole-prefix table extent scans when stripping consumed reference definitions.
2. Reference candidate guards: skip expensive reference-collection checks on lines that cannot contain reference, table, quote, HTML, fence, list, or setext syntax.
3. Nested reference allocation pass: reduce temporary `Vec<String>` and `Vec<&str>` churn in nested blockquote/list reference harvesting while preserving scope.
4. Paragraph classifier fast paths: make `line_is_paragraph_text` and related block-starter checks return early with byte-level predicates before full semantic detectors.
5. Spanned parser span collection: apply the proven reference/table boundary optimization to source-span collection and diagnostics path.
6. Inline parse allocation reduction: target `inline_parse` object churn without changing delimiter pairing, link resolution, or autolink behavior.
7. Table parser row splitting: reduce allocation/copying in GFM table splitting and delimiter validation.
8. List splitting body storage: reduce list-item body string cloning while preserving lazy-continuation, looseness, and task-list semantics.
9. Line split/source line storage: evaluate line splitting and source-line span storage for avoidable allocations in parser-large and spanned parse.
10. PDF layout hot loop: target text measurement, paragraph item construction, or layout scratch reuse in the `layout` stage.
11. PDF page content generation: reduce buffer churn and repeated formatting in `page_content_stream_generation`.
12. PDF stream compression policy: check compression thresholds and staging to avoid work where Flate cannot win.
13. PDF object serialization writers: target deterministic append writers in `pdf_object_serialization_total` and xref/trailer emission.
14. Hyphenation corpus path: look for a scored cache/layout opportunity in the 29.779 ms p95 `hyphen-corpus` scenario.
15. Final reprofile and guardrail pass: rerun focused baselines, update artifacts/proofs, and only accept changes that clear behavior and variance gates.

## Completed Passes
### Pass 1 - Reference consumed-table boundary precompute - 2026-07-05T16:35:00Z
- Files changed: `src/parse/mod.rs`
- Changes: precomputed table-end boundaries once for consumed reference stripping and spanned source-span collection, replacing repeated whole-prefix `table_extent` rescans.
- Verification: `cargo test refdef_paragraph_tests --lib`; `cargo test --test parser_conformance reference`; `cargo test --test parser_spans reference`; `cargo fmt --check`; `cargo check --all-targets`.
- Isomorphism: kept-line ordering is unchanged; table-separation decision remains the same existential table-end predicate; no floating-point or RNG involved.
- Commit: `5d84910`
- Verdict: PRODUCTIVE

### Pass 2 - Reference candidate guards - 2026-07-05T16:44:00Z
- Files changed: `src/parse/mod.rs`
- Changes: guarded both flat and nested reference-definition parser calls with `looks_like_reference_definition`, avoiding the allocating `parse_reference_definition` path on ordinary block-boundary lines.
- Verification: `cargo test refdef_paragraph_tests --lib`; `cargo test --test parser_conformance reference`; `cargo test --test parser_spans reference`; `cargo fmt --check`; `cargo check --all-targets`; `cargo clippy --all-targets -- -D warnings`; `cargo test`.
- Isomorphism: all accepted definitions still pass through the same parser; non-candidate lines already returned `None`, and the guard is a necessary precondition for a valid reference-definition opener.
- Commit: `714f8f5`
- Verdict: PRODUCTIVE

### Pass 3 - Nested reference consumed-bitmap elision - 2026-07-05T16:49:00Z
- Files changed: `src/parse/mod.rs`
- Changes: split the reference collector into a shared core that can run with or without a mutable consumed-lines bitmap, and routed nested blockquote/list reference harvesting through the map-only path.
- Verification: `cargo test refdef_paragraph_tests --lib`; `cargo test --test parser_conformance reference`; `cargo test --test parser_spans reference`; `cargo fmt --check`; `cargo check --all-targets`; `cargo clippy --all-targets -- -D warnings`; `cargo test`; `cargo build --no-default-features`.
- Isomorphism: top-level collection still allocates and fills the same consumed bitmap; nested collection never used that bitmap and still merges the same first-definition-wins reference map.
- Commit: `e3cf7ae`
- Verdict: PRODUCTIVE

### Pass 4 - Paragraph classifier scanner fast path - 2026-07-05T16:55:00Z
- Files changed: `src/parse/mod.rs`
- Changes: used the existing conservative scanner oracle to fast-accept obvious paragraph lines before running the full block-starter detector chain in `line_is_paragraph_text`.
- Verification: `cargo test line_classifier_distinguishes_text_from_block_openers --lib`; `cargo test --test scanner_test markdown_line_scanner`; `cargo test --test parser_conformance`; `cargo fmt --check`; `cargo check --all-targets`; `cargo clippy --all-targets -- -D warnings`; `cargo test`; `cargo build --no-default-features`.
- Isomorphism: fast acceptance is disabled for leading whitespace and all scanner-reported block-starter candidates, so tab/indent, quote, list, fence, HTML, heading, and thematic-break lines still use the old semantic checks.
- Commit: `cc13984`
- Verdict: PRODUCTIVE

### Pass 5 - Spanned extent-only table and indented-code spans - 2026-07-05T17:02:00Z
- Files changed: `src/parse/mod.rs`
- Changes: stopped spanned span collection from building temporary line slices and full table/indented-code parse results when it only needs line extents; introduced shared extent helpers and removed the now-dead private `parse_table` wrapper.
- Verification: `cargo test --test parser_spans`; `cargo test --test parser_conformance spanned`; `cargo test --test parser_conformance indented`; `cargo test --test parser_conformance gfm_table`; `cargo fmt --check`; `cargo check --all-targets`; `cargo clippy --all-targets -- -D warnings`; `cargo test`; `cargo build --no-default-features`.
- Isomorphism: table spans still use the same delimiter/body extent rules as table parsing, and indented-code extent preserves the parser's trailing-blank handling before code text is constructed.
- Commit: `94c0325`
- Verdict: PRODUCTIVE
