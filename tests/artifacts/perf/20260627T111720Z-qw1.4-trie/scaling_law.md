# Scaling Law

Current run establishes single-process stage timings. Batch-100 CLI timing is in `hyperfine.json`.

For future Asupersync batch mode, use `rho = lambda / (c * mu)` and keep rho <= 0.70 for latency-sensitive interactive mode or <= 0.85 for throughput mode.
Single large documents should parallelize independent paragraph/code/font-face work while preserving serial page-building until partition certificates exist.
